# 물류자동화 5종 물동량 계산 프로그램 개발계획서

독립 프로그램 개발·검증 후 통합 플랫폼 구축

| 구분 | 내용 |
| --- | --- |
| 문서 버전 / 작성일 | v1.2 / 2026-09-09 |
| 개발 대상 | ① 스태커크레인 ② 직선 RGV ③ Loop RGV ④ 4-way Shuttle ⑤ 컨베이어 |
| 작성 목적 | 기존 계산 자산을 활용한 5종 독립 프로그램의 개발 범위, 계산 기준, 검증 기준, 완료 조건 및 후속 통합 방법 정의 |
| 기준 자료 | 첨부 STC 기술자료 v1.3, 통합 플랫폼 PRD v1.4, 공개 시뮬레이션·최적화 자료, 통합 플랫폼 PRD v1.0(2026-08-30), STC FlexSim형 시뮬레이터 구현계획 v1.0(2026-09-07) |
| 문서 상태 | 개발 착수·범위 협의를 위한 제안 기준선. 일정과 수치 합격 기준은 착수 검토 후 확정 |
| 이번 개정 범위 | PRD v1.0의 규격 적용 원칙·지연 모델·보정계수·대수산정·골든 테스트·금지사항과 STC 시뮬레이터 구현계획의 DES 엔진·3D 애니메이션·통계·실험기·최적화기 설계를 5종 공통 기능과 장비별 범위에 반영. 시뮬레이션 구현 순서와 검증 기준을 구체화 |

### 개정 이력

| 버전 | 일자 | 주요 변경 |
| --- | --- | --- |
| v1.0 | 2026-09-08 | 5종 독립 개발 후 통합 방침, 공통 계산 기준, 장비별 범위, 일정 초안 |
| v1.1 | 2026-09-09 | 장비별 DES 구현 범위, 시뮬레이션 아키텍처(S0~S4), 검증 절차, 통합 2차 연결 시뮬레이션 |
| v1.2 | 2026-09-09 | 첨부 A4(PRD v1.0)·A5(STC 시뮬레이터 구현계획) 반영: 규격 준용 표기, 지연·보정계수·대수산정 커널, 실험기·최적화기·3D 애니메이션, 연속 보간 재생, 공통난수, 프리셋·검증 사례, 골든 테스트, 금지사항, 공수 재산정. 반영 대조는 부록 B |

## 1. 개발 방향 요약

### 1.1 핵심 결정

**5개 프로그램은 각각 실행·설정·저장·계산·검증할 수 있도록 완성한 뒤 하나의 프로그램으로 통합한다. 다만 단위, 공통 운동 계산, 데이터 규격, 결과 지표는 개발 시작 시점부터 통일한다.**

통합 시 화면만 모아 놓는 단계와 실제 장비 간 물류를 연결하는 단계는 작업량과 검증 범위가 다르므로 구분한다.

| 단계 | 목표 | 완료 시 사용자에게 제공되는 것 |
| --- | --- | --- |
| 공통 준비 | 기존 자산 확인, 계산 기준선·공통 인터페이스 확정 | 공통 계산 커널, 데이터 규격, 검증 사례 |
| 독립 개발 | 5종별 설정·분석 계산·장비 단위 DES·통계 대시보드·실험기·보고서 구현 | 각각 단독 실행되는 5개 프로그램과 재현 가능한 시뮬레이션·반복 실험 |
| 통합 1차: 단일 프로그램 | 5종 모두 단독 완료 승인 후 프로젝트·화면·저장·보고서 통합 | 하나의 실행 파일에서 5종 계산 및 결과 비교 |
| 통합 2차: 연결 시뮬레이션 | 승인된 장비 플러그인을 전역 시계·화물·버퍼·포트로 연결 | 전체 라인 처리량, 병목, 대기, WIP, 필요 설비 수 검증 |
| 고도화 | 데이터 기반 보정, 설계 탐색기 확장, AI 운영정책 비교 | 검증된 조건 안에서 설비 구성·운영안 추천 |

장비 단위 DES는 5종 독립 프로그램의 출시 범위에 포함한다. 통합 2차는 장비별 시뮬레이션이 승인된 뒤 연결 계약을 검증하는 별도 산출물·일정으로 관리한다. AI 학습 완료를 시뮬레이션 또는 5종 프로그램의 통합 착수 조건으로 삼지는 않는다.

시뮬레이션 기능의 기준은 첨부 A5가 정의한 FlexSim형 4요소, 즉 **애니메이션 뷰, 통계 대시보드, 실험기(Experimenter), 최적화기(Optimizer)** 로 한다. 이 중 통계 대시보드와 실험기는 5종 공통 필수이고, 설계 탐색용 최적화기는 공통 모듈로 구현하되 장비별 설계공간 정의가 승인된 장비부터 활성화한다. 3D 애니메이션은 스태커크레인에서 A5 설계를 그대로 구현하고, 나머지 4종은 동일한 렌더 모델을 소비하는 2D 뷰를 기본으로 한다. [첨부 A5]

### 1.2 제품이 답해야 하는 질문

- 주어진 시간대별 입고·출고 물동량을 현재 설비가 처리할 수 있는가?
- 설비별 이론 처리량과 운영 조건을 반영한 처리량은 얼마나 다른가?
- 목표 물동량에 필요한 크레인·RGV·셔틀·리프트·컨베이어 존 수는 얼마인가?
- 대기행렬과 버퍼가 어디에 얼마나 쌓이고, 피크 구간 이후 회복되는가?
- 속도 향상, 설비 증설, 버퍼 증설, 배차·입출고 순서 변경 중 무엇이 효과적인가?
- 계산 결과의 근거와 가정, 표준 적용 범위, 미검증 항목을 추적할 수 있는가?

### 1.3 기본 기술 방향

첨부 PRD의 오프라인 단일 HTML/JavaScript 배포 방향을 우선 유지한다. 사용자는 브라우저에서 파일을 열어 계산하고 JSON으로 프로젝트를 저장한다. 개발 단계에서는 소스 모듈을 분리하고 배포 단계에서 묶는다.

Python은 기존 계산기와의 비교 검증, 선택적 배치 실험·AI 학습에 사용한다. 운영용 분석·DES 엔진은 JavaScript를 기준으로 하나만 유지하고, Python은 독립 검산기·오프라인 실험기·정책 학습 연계용으로 분리한다. 공개 프로젝트의 코드를 제품에 직접 복사하지 않고, 이벤트 큐·상태·자원·정책 분리와 반복 실험 구조만 참고한다. [첨부 A2·A3]

외부 라이브러리는 3D 뷰의 three.js(r128 UMD) 한 가지로 제한한다. CDN 참조 대신 저장소에 vendor 사본과 SHA-256을 기록해 오프라인 단일 HTML에 인라인하고, 빌드 시 해시를 대조한다. 회사망에서 다운로드가 막히면 npm pack 경로로 확보한다. 그 외 모듈은 프레임워크 없이 IIFE 전역 1개씩으로 작성하고, Node 환경에서는 `require`로, 브라우저에서는 전역으로 동일 소스를 읽는다. [첨부 A5]

## 2. 자료 검토 및 기존 자산 활용

### 2.1 근거와 확인 수준

| 근거 구분 | 의미 | 계획서에서의 취급 |
| --- | --- | --- |
| 첨부 A1~A5 | 사용자가 제공한 기술자료·PRD(v1.4, v1.0)·공개 코드 조사·STC 시뮬레이터 구현계획 | 요구사항, 기존 개발 이력, 시뮬레이션 설계·참고 구조의 기준 |
| 공개 R1~R11 | 공식 저장소·공식 문서 확인 | 시뮬레이션 구조·알고리즘·검증 방법의 참고 |
| 본 계획 제안 | 이번에 정리한 설계·일정·합격 기준 | 담당자 승인 후 기준선으로 확정 |
| 추가 확인 필요 | 원본 코드·시험 로그·규격 원문·실측 미확인 | 검증 과제로 등록하며 확인 완료로 표시하지 않음 |

첨부 문서에 적힌 구현 상태와 테스트 통과 수는 **문서 기재 내용**이다. 이번 작업에서는 실제 프로젝트 소스·실행 파일·시험 로그를 전달받아 재실행하지 않았으므로 현재 재현 여부를 보증하지 않는다. 공개 프로젝트 역시 자료 검토이며 실행 성능이나 현장 적합성을 검증한 것은 아니다.

### 2.2 재사용 후보

| 영역 | 첨부 문서의 기존 자산 | 재사용 방향 | 착수 시 확인 |
| --- | --- | --- | --- |
| 공통 커널 | MHKernel v0.1.0, 25,013개 검증 기재 | 운동·단위·시간 합산·물리 유효성 검사의 기준 후보 | 실제 소스, 테스트 정의, 빌드 결과, 해시 |
| 스태커크레인 | Python/Tkinter v2.0.0, Excel 5종, 별도 STC DES v0.1, FEM 포팅 11개 골든 사례 기재 | 검증된 부분의 플러그인화, 분석식과 DES 연결 | creep·지연 0 조건 외의 일치 여부, 미구현 옵션 |
| 직선 RGV | HTML/JS v0.1, 16개 자체 시험 기재 | 기존 UI·입력·커널 기반으로 가장 작은 표준 플러그인 확보 | 왕복·공차·구간 인계의 정의 |
| Loop RGV | Python 계산기, 35개 시험·Excel 검증 43개 기재; 별도 HTML 계산기·WCS 시뮬레이터 계열 | 기능·산식 대조 후 기준 계열 선정 및 통합 포팅 | 분기 계보, 단위, 구간 계산, 혼잡 보정 차이 |
| 4-way Shuttle | HTML v2, FEM 부속서 관련 약 50개 시험, 시나리오·시각화 기재 | 분석 계산과 입력 자산 활용, 교차로·리프트 경합 모델 보강 | 실제 4방향 이동 지원 범위, 필요 대수 산식 |
| 컨베이어 | cvcore 2.0 / app 2.0.1, 포팅 명세 6종, 골든 데이터 2,617건 및 의도된 예외 6건 기재 | 계산 코어 포팅, 데이터 기반 회귀시험, 기존 보고서 활용 | Excel과 cvcore 중 기준본, 반올림·예외의 승인 이력 |
| 시뮬레이션 설계 | STC FlexSim형 시뮬레이터 구현계획 v1.0(A5): 엔진·운동학·통계·실험기·최적화기·3D 뷰·데이터 계약·테스트 목록 | SimulationCore, ExperimentRunner, Optimizer, RenderModel의 설계 기준. STC 플러그인 DES의 구현 순서로 채택 | 구현 착수 전 문서이므로 코드 없음. 커널 사본 위치·버전과 프리셋 수치는 원본 docx 확인 후 확정 |
| 빌드·역검증 | `02_rgv_linear/src/build.js`, `template.html`: 마커 인라인 → 배포물에서 스크립트 역추출 → 동일 테스트 재실행 | 5종 공통 shipcheck 절차. 모듈별 `data-module` 속성 기반 역추출로 교체 | 블록 개수 검사가 아니라 속성 기반 검사인지 확인 |
| Worker·재생 UX | `rgv-throughput-sim/index.html`, `loop_rgv_통합_v3.0.html`의 `runSweep`(Blob Worker + 동기 폴백), T95 표, 집계·렌더 분리 | 실험기 Worker 풀·폴백·진행률·KPI 카드·상태시간표 패턴 | Worker 미허용 환경의 폴백 동작 재현 여부 |
| STC 프리셋 원천 | `STC 물동량 계산서(FEM 9.851 준용)_Rev0.docx`, 5 bay×6 level 차체 저장 설비 | 검증 사례·프리셋 수치 확정. 범용 기본값으로는 승격하지 않음 | t_ü 8 s의 의미, 252%에 η 포함 여부, bay 피치·단 높이, 양측 랙 여부 |

기존 폴더 번호는 컨베이어 04, 4-way Shuttle 05로 기재되어 있다. 사용자 화면과 본 계획의 순서는 4-way Shuttle ④, 컨베이어 ⑤로 하되, 기존 폴더를 무조건 변경하지 않는다. 파일 경로와 장비 식별자를 분리하여 이력과 참조를 보존한다. [첨부 A2]

### 2.3 선행 해소 과제

| ID | 확인된 문서상 쟁점 | 개발 조치 |
| --- | --- | --- |
| D01 | 셔틀 필요 대수 식과 리프트/셔틀 처리량 비율의 설명이 서로 불일치 | 자원별 처리량 정의부터 재유도하고 정수 대수·골든 사례를 재검증 |
| D02 | FEM 대표 좌표·일부 부속서 해석·VDI 관련 오차 기준의 원문 확인 필요 | 원문 버전·조항·기호 대응표 확보 전 “규격 준수” 확정 표시 금지 |
| D03 | STC FEM 계산의 11개 일치 사례는 creep·추가지연 0인 제한 조건 | legacy 동작과 신규 표준 동작을 구분하고 비영 조건 회귀시험 추가 |
| D04 | Loop RGV의 거리 미소 변화에 대한 이동시간 불연속 사례 기재 | 임계거리 전후 연속성·단위·분기·중간 반올림을 우선 조사 |
| D05 | 컨베이어 Excel·cvcore 간 차이 및 중간 올림·내림 존재 | 기준본·의도된 차이 목록 승인 후 포팅; 차이를 조용히 삭제하지 않음 |
| D06 | 가용도·혼잡 보정과 시뮬레이션 대기시간의 중복 반영 가능성 | 분석 보정과 명시적 이벤트 모델을 구분하고 이중 적용 방지 |
| D07 | 일부 입력 옵션이 표시되지만 실제 엔진에 반영되지 않는 이력 | 옵션별 지원 상태를 명시; 미지원 값은 차단 또는 경고 |
| D08 | 평균 대기와 최대 대기 필드 혼용, 부적합 설계가 우선 정렬되는 문제 | mean/P95/max 분리, 제약 충족안만 추천 순위에 진입 |
| D09 | 분석 보정과 DES 이벤트 손실의 적용 경계가 불명확 | 분석 모드와 DES 모드를 분리하고 동일 손실의 이중 반영을 금지 |
| D10 | 장비별 DES와 통합 라인 DES의 완료 조건이 혼재 | 장비 단위 Gate와 연결 시뮬레이션 Gate를 별도로 정의 |
| D11 | FEM 9.860 §1은 이송대차·AGV를 적용 범위에서 제외하므로 직선/Loop RGV는 셔틀 수식의 "준용"이며, 기존 핸드북에 ISO 3691-4 표기 오류 기재 | RGV 보고서에 "FEM 9.860 준용, 규격 적용대상 아님"을 고정 문구로 출력하고 안전규격은 EN 619:2022로 표기. ISO 3691-4는 레일 유도 차량을 Scope에서 배제하므로 사용 금지 [첨부 A4] |
| D12 | Loop RGV 기존 계산기에 저대수 구간 초선형(N=8에서 선형 대비 1.39배, 정점 N≈empty1/blockSize)과 `utilization`이 포화 지표가 아닌 결함(차량 병목이면 항상 1.0) 기재 | 대수 검토범위를 정점 너머까지 강제 확장해 포화 구간을 화면에 표시하고, 여유 판단 지표로 목표/달성용량 비율을 별도 출력. 회귀시험 기대값이 바뀌는 사례는 사전 식별하고 사유를 테스트에 기록 [첨부 A4] |
| D13 | 컨베이어 기존 규칙 "같은 장치명끼리 1대 공유"와 본 계획의 고유 resourceId 원칙이 충돌 | 기존 호환 모드에서는 이름 기반 공유를 유지하되 변환 시 경고를 출력하고, 신규 정밀 모드는 명시적 resourceId 참조만 허용 [첨부 A4] |
| D14 | STC 차체 저장 설비 프리셋의 t_ü 8 s 의미(이동당 위치결정/포크 1회/사이클당 t03~t05), 252%에 η 포함 여부, bay 피치·1단 높이·양측 랙, 출고 take-away 속도, 출고 전용 리프트 대안의 크레인 작업 변화가 미확정 | G0에서 원본 docx 본문을 추출해 확정. 미확정 시 A5의 기본 가정(이동당 tPos, η 미포함, 균등 분할, 양측, 무한 싱크, 출고 ST y 8→0)을 적용하고 프리셋에 "가정" 표시 [첨부 A5] |
| D15 | PRD v1.0은 보정계수 A·F_t·E_w 기본값 1.0을 금지하고, 본 계획 3.4절은 가용도 1을 "이상 조건 명시 시"에만 허용 | 두 규칙을 통일한다: 보정계수는 기본값 없이 사용자가 값과 근거(실측/벤더/가정)를 선택해야 계산이 진행되며, 1.0 선택 시 "이상 조건" 라벨이 보고서에 남는다. 민감도 분석은 기본 제공 [첨부 A4] |

## 3. 전 프로그램 공통 계산·데이터 기준

### 3.1 입력·단위·결과 분리

| 범주 | 공통 정의 |
| --- | --- |
| 운동 내부 단위 | 거리 m, 속도 m/s, 가·감속도 m/s², 시간 s |
| 화면 입력 단위 | mm·m, m/min·m/s 선택 가능; 입력 경계에서 한 번만 변환 |
| 물동량 단위 | PLT/h, box/h, tote/h 등을 화물 유형과 함께 기록 |
| 단위 변환 | 박스↔팔레트는 명시적 적재·분할 비율과 공정이 있는 경우만 허용 |
| 수요 입력 | 평균·시간대별 수요, 입고/출고 구분, 피크 계수, 작업일·교대·운영시간 |
| 수요 생성 | 일정 간격, 확률 분포, CSV 작업 이력; 모드와 생성 근거 기록 |
| 제품·화물 | 크기·중량·취급 단위·SKU·적재 수량·설비 적합성 |
| 장비 사양 | 레이아웃, 속도·가감속, 적재/공차 차이, 이재시간, 정위치, 제어 지연 |
| 운영 조건 | 배차·저장 위치·출고 순서, 초기 재고/WIP, 버퍼, 고장·휴지 시간 |
| 분석 결과 | 이론 처리량, 보정 처리량, 요구량, 부족량, 설계 여유 |
| 시뮬레이션 결과 | 완료량/측정시간, 대기·차단·공차·가동시간, 재고/WIP, 미완료 작업 |

보유량·하루 총량·시간당 설비능력을 서로 다른 필드로 관리한다. 일 물동량을 운영시간으로 나눈 값은 평균 수요이며 설비의 피크 대응능력을 뜻하지 않는다.

### 3.2 공통 운동 커널

정지 상태에서 출발하여 다시 정지하는 1축 구간의 기준식을 다음과 같이 둔다. 거리 s, 제한속도 v, 가속도 a, 감속도 d는 양의 물리량이다.

$$
s_{\mathrm{crit}}=\frac{v^2}{2}\left(\frac{1}{a}+\frac{1}{d}\right)
$$

$$
t(s)=
\begin{cases}
0 & s=0 \\
\sqrt{\dfrac{2s}{1/a+1/d}}\left(\dfrac{1}{a}+\dfrac{1}{d}\right) & 0<s<s_{\mathrm{crit}} \\
\dfrac{s}{v}+\dfrac{v}{2}\left(\dfrac{1}{a}+\dfrac{1}{d}\right) & s\geq s_{\mathrm{crit}}
\end{cases}
$$

적용 조건과 예외:

- 제한속도·가감속도가 0 이하이거나 거리가 음수인 입력은 유효성 오류로 처리한다.
- 여러 구간의 속도 제한이 다른 경우 경계속도를 연결하여 계산한다. 단순히 구간을 잘랐다는 이유로 매번 정지·재출발시키지 않는다.
- 실제 정지점에서는 정지 조건을 적용하고, 연속 구간에는 전진·후진 검토로 가능한 경계속도를 산정한다.
- STC의 독립적인 수평·수직 동시 이동은 max(수평시간, 수직시간)를 사용할 수 있다. 리프트 인계, 셔틀 방향 전환 등 순차 동작에 이 식을 적용하지 않는다.
- creep, jerk, 정위치, 이재, 인터록은 적용 위치·횟수를 각각 기록한다. 사용자가 총 이재시간을 지정하면 세부 항목의 합을 대체하며 다시 더하지 않는다.
- jerk를 정밀 모델링하지 않은 근사식은 근사 모드로 표시한다. 임의의 공통 가산율을 검증된 물리값처럼 사용하지 않는다.
- jerk 처리는 FEM 9.860 §5.2.2의 두 방식 중 하나를 입력으로 선택한다. ⓐ 전 구간에 약간 낮춘 일정 가속도를 가정하거나, ⓑ 최대 가속도를 사용한 뒤 jerk 시간을 가산한다. 선택한 방식과 값은 결과 근거에 기록한다. [첨부 A4]
- a = d이면 위 식은 FEM 9.860 식(3)과 일치한다. s ≥ v²/a에서 t = s/v + v/a, s < v²/a에서 t = 2√(s/a)이다. 이 경우를 커널 단위시험의 기준 사례로 둔다. [첨부 A4]
- 저속 접근(creep) 구간은 켜고 끌 수 있는 선택 구간이며, 켜고 끄는 경계와 creep 거리 변경 시에도 이동시간이 연속이어야 한다. Loop RGV의 d_fixed 불연속(D04)이 이 경계에서 생겼으므로 최우선 방지 대상이다. [첨부 A4]
- 커널은 총 시간뿐 아니라 가속·순항·감속 구간 목록(phases)을 반환한다. 시뮬레이션 재생은 이 구간 목록으로 임의 시각 τ의 위치 posAt(τ)를 구간별 2차식으로 보간하며, 별도의 운동 계산을 두지 않는다. posAt(0) = 0, posAt(T) = s, 단조 증가, 구간 경계 속도 연속을 시험으로 고정한다. [첨부 A5]
- 2축 동시 이동 makeMove는 두 축이 동시에 출발하고 dur = max(tX, tY) + tPos로 계산하며, 빠른 축은 도착 후 정지 상태를 유지한다. 적재 시 속도·가감속 저감은 loadedFactor 훅으로 두되 기본값은 1이다. [첨부 A5]

이 기준은 첨부 공통 커널의 방향을 따르며, 실제 API와 예외 처리는 소스 확인 후 확정한다. [첨부 A1·A2]

#### 지연 모델

축 이동시간 외의 부수 시간은 항목별로 분리 입력하고 합산한다. 항목은 제어 지연, 통신 지연, 위치결정(positioning) 지연, 센서 감지, PLC 스캔, 모터 기동, 스토퍼/디버터 승강, 안전 여유다. 간편 입력(합계 1값)과 상세 입력(항목별)을 모두 지원하되, **상세 항목 합의 기본값은 간편 입력 기본값과 같아야 한다.** 상세 모드를 켜는 것만으로 결과가 바뀌면 안 된다. 각 항목은 적용 위치(이동당, 정지당, 포크 1회당, 사이클당)와 횟수를 함께 기록하며, 3.2절의 "총 이재시간 지정 시 세부 합을 대체" 규칙과 결합한다. [첨부 A4]

### 3.3 처리량 집계 규칙

SC는 단일 명령 사이클, DC는 입고·출고를 조합한 이중 명령 사이클로 정의한다. 이 정의는 장비별 실제 동작과 연결하여 표시한다.

| 결과 항목 | 집계 기준 |
| --- | --- |
| cycle/h | 완료된 설비 사이클 수 |
| 입고 PLT/h | 완료된 입고 화물 수 |
| 출고 PLT/h | 완료된 출고 화물 수 |
| 총 취급 PLT/h | 입고+출고 등 명시한 취급 작업의 합 |
| 라인 출하 PLT/h | 외부 출하 경계에서 완료된 화물 수; 중간 설비 취급량을 합산하지 않음 |
| 주문 달성률 | 측정 대상 주문 중 납기·수량 조건을 충족한 비율 |
| 미완료 작업 | 대기·운송·처리 중 작업을 구분하여 별도 보고 |

1 PLT SC는 보통 1건의 취급, 입고 1건+출고 1건인 DC는 2건의 취급이다. DC 1회가 출고 2건인 것은 아니다. 다중 포크·다중 적재도 실제 적재·완료 수를 사용하며 일괄 2배 처리하지 않는다.

DC 비율 p를 **전체 사이클 중 DC 사이클의 비율**로 정의하고, SC 1개·DC 2개 화물 취급 조건일 때:

$$
Q_{\mathrm{total}}=
\frac{3600(1+p)}{(1-p)T_{\mathrm{SC}}+pT_{\mathrm{DC}}}
$$

입고·출고 구성은 별도 검증한다. 거래 건수 기준 비율을 위 p에 그대로 대입하지 않는다. 빈도 가중 평균과 처리량의 평균도 혼동하지 않는다.

### 3.4 보정·가용도·설계 여유

- 이론능력 Q_theory, 분석 보정능력 Q_available, 실제 이벤트 완료 기준 Q_sim을 별도로 표시한다.
- 분석 모드에서 Q_available = Q_theory × 가용도 × 승인된 보정계수를 사용할 수 있다. 계수마다 의미·근거·적용 위치를 기록한다.
- 시뮬레이션에서 이미 고장·정지·혼잡을 이벤트로 구현했다면 같은 손실을 계수로 다시 차감하지 않는다.
- 가용도 1은 “고장·계획정지 없는 이상 조건”으로 명시할 때만 사용한다. 임의의 현장 실측값으로 표현하지 않는다.
- 평균 요구량 λ_avg와 피크 계수 f_peak가 있으면 λ_peak = λ_avg × f_peak이다. 필요 시 설계 여유계수 k_design ≥ 1을 추가하여 Q_available ≥ λ_peak × k_design 여부를 판단한다.
- 이미 피크 시간대 원시 작업 이력을 넣은 경우 같은 피크 계수를 다시 곱하지 않는다.
- 부하율 λ/Q와 관측 가동률 busy time/측정시간은 구분한다. 부하율은 1을 넘을 수 있지만 단일 자원의 시간 비율은 100%를 넘지 않는다.
- “80% 이상이면 항상 불안정” 같은 공통 단정은 사용하지 않는다. 허용 부하율·대기시간·서비스 수준은 수요 변동성과 설비 특성에 따라 정한다. 다만 목표 부하율 ρ_max는 프로젝트 설정값으로 두고 기본 제안값 0.8과 그 근거(대기시간 발산 경향)를 화면에 표시한다. 초과 시 경고하되 오류로 차단하지 않는다. [첨부 A4]

#### 보정계수 정의

분석 모드의 보정계수는 FEM 9.222 / OPIL 체계의 세 계수로 나누어 입력한다. [첨부 A4]

| 기호 | 의미 | 범위 | 비고 |
| --- | --- | --- | --- |
| A | 가동률 | (0, 1] | 고장·정비·충전 제외 시간 비율 |
| F_t | 교통계수 | (0, 1] | 혼잡·간섭 보정. 실측이 있으면 F_t = T_이론 / T_실측으로 역산 |
| E_w | 작업효율 | (0, 1] | 완전자동 설비는 1.0 |

필요 대수는 1/(A·F_t·E_w)에 비례하므로 어느 하나가 5% 줄면 소요 대수가 약 5% 늘어난다. OPIL 예제에서 세 계수를 1.0으로 두면 3대가 1대로 오산된다. 따라서 D15에 따라 기본값을 제공하지 않고, 사용자가 값과 근거를 선택해야 계산이 진행된다. 각 계수를 ±5·10%씩 흔든 민감도 표를 기본 결과에 포함한다. 시뮬레이션이 고장·혼잡을 이벤트로 구현한 경우 같은 손실을 A·F_t로 다시 차감하지 않는다(D06·D09).

#### 대수산정 커널

장비 종류와 무관한 대수산정 함수(fleet)를 공통 커널에 둔다. 총 반송 수요 w = ΣF_ij [건/h], 사이클타임 T_C [min]일 때:

$$
WL = w\,T_C,\qquad
AT = 60\,A\,F_t\,E_w,\qquad
AN = \left\lceil \frac{WL}{AT} \right\rceil
$$

$$
\theta_N = \frac{60\,N\,A\,F_t\,E_w\,\eta(N)}{T_C},\qquad
\rho = \frac{WL}{N\cdot AT}
$$

WL은 시간당 작업부하 [min/h], AT는 차량 1대의 가용시간 [min/h], AN은 소요 대수, θ_N은 N대 달성 물동량, η(N)은 혼잡 간섭계수(6.3절)다. 리프트가 병목인 구성의 FEM 9.860 §5.6 경로(λ_y = 3600·n_L / t_C2,y, λ_x = 3600 / t_C2,x)는 7.3절의 균형식과 같은 함수에서 제공한다. OPIL 예제 1·2를 골든 사례(12.2절 K08)로 고정한다. [첨부 A4]

### 3.5 공통 저장 규격

| 객체 | 필수 정보 |
| --- | --- |
| Project | projectId, schemaVersion, 프로젝트명, 작성·수정일, 단위 정책 |
| ModuleConfig | moduleId, deviceType, moduleVersion, kernelVersion, 입력, 지원 기능 |
| Scenario | scenarioId, 수요, 초기 상태, 정책, 기간, seed, warm-up 설정 |
| Load / Job | loadId, jobId, 화물 단위·수량, 출발·도착, 도착시각, 납기 |
| Resource / Port | 고유 ID, 소유 모듈, 위치, 용량, 화물 적합성, 인계 규칙 |
| Result | 계산 방식, 주요 KPI, 시간 분해, 경고, 원시 완료량, 측정 구간 |
| Provenance | 모델·커널 버전, 입력 해시, 실행 시각, 근거식 버전, 적용 가정 |
| RepResult | 반복 1회의 집계값만: seed, 측정시간, 작업 카운터(생성·완료·대기·진행·서비스불가), 처리량(전체·입고·출고·요구·달성률), 자원별 상태시간·이동 수·거리·사이클 통계, 스테이션별 도착·서비스·평균/최대 큐·평균/P95 대기·차단시간·백로그, 재고 통계, 고정빈 히스토그램, 시계열 빈, 플래그(saturated, queueTrend), 해석식 대조값 |
| ScenarioResult | 시나리오 ID, 설계 변수, 반복 수 n, 지표별 {mean, sd, ci95, n}, 히스토그램 합, 시계열 평균, 실행가능 여부, 실패 사유 목록 |
| DesignSpace / OptimizerRow | 설계 변수별 후보 집합; 설계, 유효성·무효 사유, 해석식 사전필터값, 스크리닝 단계, 결과, 순위, Pareto 소속 |

단독 프로그램도 처음부터 이 규격을 사용한다. 오래된 JSON은 버전별 변환기로 읽고, 원래 파일을 자동 덮어쓰지 않는다. 숫자 변환 실패, 잘못된 단위, 모르는 주요 버전, 지원하지 않는 조합은 명시적으로 거절한다. 버전 비교는 major.minor만 대조하여 경고하고 패치 차이로는 묻지 않는다. [첨부 A4]

Scenario에는 seedBase와 실행 설정(durationSec, warmupSec, tsBinSec, sampleSec)을 두고, 반복 r의 seed는 seedBase + r로 고정한다. Worker는 작업 로그가 아닌 RepResult만 반환하고, 메인 스레드가 이를 ScenarioResult로 접는다. 스키마 이름은 `<device>/scenario@1`, `<device>/rep@1` 형식으로 장비별 버전을 분리한다. [첨부 A5]

### 3.6 개발 금지사항

다음은 문헌 근거가 있는 금지 규칙이며, 코드 리뷰와 회귀시험 항목으로 강제한다. [첨부 A4]

| # | 금지 | 근거 | 본 계획의 처리 |
| --- | --- | --- | --- |
| 1 | 처리능력을 대수에 선형 비례로 계산 | Calzavara et al. 2018, 포화 미표현 | η(N) 또는 DES로 포화 표현(6.3절) |
| 2 | 주행시간을 s/v로 계산 | FEM 9.860 식(3), 짧은 구간 과대평가 | 커널 t(s) 외 자체 구현 금지(9.1절) |
| 3 | 공차 주행 무시 | 단방향 루프는 배송당 평균 1랩 | 적재·공차 경로 분리(5.3·6.3절) |
| 4 | 보정계수 A·F_t·E_w를 1.0으로 방치 | OPIL 예제 3대 → 1대 오산 | 기본값 없음, 근거 선택 강제(D15) |
| 5 | 곡선구간 속도제한 미반영 | Loop형은 U턴 필연(회전반경 예: 750 mm) | 구간별 속도 제한과 경계속도 연결(3.2절) |
| 6 | 대규모 대수에서 해석식만 신뢰 | 국내 OHT 연구, 시뮬레이션 병용 필수 | 장비 단위 DES 필수(9.6절) |
| 7 | ρ > 목표 부하율 설계를 경고 없이 승인 | 대기시간 발산 | ρ_max 설정값 초과 시 경고(3.4절) |
| 8 | 토폴로지 무시하고 단일 모델 적용 | 토폴로지별 간섭모델 상이 | 토폴로지별 확장 관리(6.2절) |
| 9 | 규격 기준점 P1/P2 혼용 | FEM 9.851 ≠ VDI 3561 | 좌표·판본 표시(4.3절) |
| 10 | 평균 사이클타임만으로 물동량 판단 | VDI 3561 Bl.4 경고문 | 사이클타임·달성 물동량·포화 지표 동시 표시(15.1절) |

## 4. 프로그램 1 — 스태커크레인

### 4.1 개발 목표

랙·크레인 사양과 입출고 운영 조건을 입력하여 SC/DC 시간, 입고·출고 처리량, 필요 크레인/통로 수, 피크 대응능력을 계산한다. 대표점 분석과 실제 셀 기반 계산, DES를 대조하여 차이의 원인을 설명한다.

### 4.2 필수 기능

| 구분 | 단독 v1.0 범위 |
| --- | --- |
| 레이아웃 | 통로 길이·높이, bay/level 좌표(균등 생성 또는 bayX/levelY 명시), 단면/양면, I/O 위치, 독립 통로별 크레인, 1통로 내 존(zone) 분할 |
| 운동 | 수평·승강 속도와 가감속, 동시 이동, creep·정위치·이재시간, 포크 사이클(2×stroke + lift), 적재 저감 훅 |
| 작업 | 입고 SC, 출고 SC, 실제 입출고 페어링 DC, 운전 모드 SC-return / SC-stay / DC 명시 구분 |
| 분석 | 대표점 방식(FEM 9.851 P1/P2), 셀 가중치·전수 열거 방식, 기존 B&W 비교 모드, FEM 9.860 식(3) 단거리 분기 옵션과 9.851 호환 결과의 병기 |
| 기존 입력 | 기존 계산기 14항목 유지: Fork 깊이(Single/Double Deep)·개수(One/Twin), Case Type 1~6, Combined 비율, STC 수량, 모터. Double Deep의 tREL, Twin Fork의 사이클당 2 pallet 규칙 |
| 운영 | 우선순위 oldest-first / storage-first / retrieval-first / alternate, 저장 규칙 random-empty / closest-open, 반출 셀 random / fifo / nearest, DC 페어링 fifo / nearest, aging 한도, 초기 충전률·초기 적재 시각 |
| 스테이션 | 입고/출고 종류, 좌표, 용량, 도착 유형 takt(±jitter) / poisson / csv, 출고 minStock 게이트, take-away 무한 싱크 또는 속도 제한, 용량 초과 백로그와 차단시간(라인 정지시간 KPI) |
| DES | 랙 셀 상태(empty / reservedIn / occupied / reservedOut), 크레인 계획 실행기, 존 귀속·수지·도달성 사전검사, 크레인 상태 7종 시간가중, warm-up 리셋, saturated·queueTrend 플래그 |
| 설계 변수 | 존 분할(single / x2 / y2 / x3 …), 스테이션 변형(as-is / 추가 출고 ST / 출고 리프트), 병렬 통로 수 parallelAisles(수요 ÷ k) |
| 시각화 | 3D 애니메이션(Three.js): 랙·크레인·스테이션·큐 스택·상태색, 배속 1~1000×, 오빗/팬/줌, 셀 클릭 정보. 동일 엔진의 advanceTo·posAt 보간 재생 |
| 실험·최적화 | 시나리오 행렬 반복 실험(R회, 95% CI), 설계공간 열거·유효성 필터·2단계 스크리닝·순위·Pareto·추천 카드 |
| 해석식 대조 | 스테이션별 tm1E/tm1A/tm2·작업부하 %와 시뮬 결과의 Δ%를 5/10% 기준색으로 표시 |
| 출력 | SC/DC 시간 분해, 입고/출고/총량, 필요 통로 수, 수요 달성, 라인 정지시간, 병목·민감도, 판정 OK/NG와 사유 |

같은 레일을 공유하는 다중 크레인과 독립 통로별 크레인은 다른 모델이다. 단독 v1.0의 기본은 독립 통로 및 통로당 1대이며, 같은 레일 다중 크레인은 별도 확장으로 표시한다. 1통로 내 존 분할은 같은 레일에 크레인이 여러 대인 경우이므로, 존 비중첩·같은 레일 존 간 x 간격 ≥ 차체폭·스테이션의 단일 존 귀속·존별 Σ입고 vs Σ출고 수지·반출 도달성 검사를 통과한 구성만 실행한다. as-is 배치처럼 어느 분할도 수지가 성립하지 않으면 "고장"이 아니라 물리 한계로 사유를 보고한다. [첨부 A5]

ABC 클래스 저장, Double-Deep 재배치 DES, 크레인 고장, 다중 통로 간 이송은 A5와 같이 범위 밖으로 두고 훅만 남긴다. Double Deep은 기존 계산기의 tREL 분석식은 유지하되 DES에서의 재배치 동작은 후속이다.

### 4.3 계산·모델 구현

1. 기존 Python·Excel·STC DES 입력 항목을 공통 스키마에 대응시킨다.
2. 주행·승강의 운동시간과 포크/이재시간을 분리하여 각각 검산한다.
3. 대표점 분석에는 적용 규격·좌표·판본·가정을 표시한다.
4. B&W 원형의 등속·균일 분포 가정과 가감속을 반영한 수정 근사 모드를 구분한다. 수정식을 원형의 정확식으로 표시하지 않는다.
5. 실제 셀별 SC는 전수 계산하고, DC 셀 조합은 규모에 따라 전수 또는 재현 가능한 표본 계산을 선택한다. 표본 수와 추정 오차를 출력한다.
6. DES에서 SC/DC 작업을 실제로 구성하고 초기 재고·대기·I/O 제약을 적용한다.
7. 목표량을 만족하는 통로 수 후보를 평가하되, 공통 입출고 설비가 있으면 단순 대수 배율로 확정하지 않는다.
8. FEM 9.851 해석식(fem9851.js)은 시뮬레이션과 같은 axisTime을 호출한다. 해석식이 5구간, 시뮬레이션이 사다리꼴이면 5% 기준이 물리 모델 차이로 깨지므로, "해석식 vs 시뮬" 차이가 순수 DES 효과만 반영하도록 한다. 기존 Python 계산기 실행값은 골든 JSON으로 고정하고 1e-6 이내 일치를 확인한다. [첨부 A5]
9. 크레인은 계획 실행기로 구현한다. 배차가 `[{move, to, loaded}, {fork, pick|place, at}, …]` 계획을 만들고, 이벤트마다 이전 동작의 효과를 적용한 뒤 다음 동작을 시작한다. 빈 계획이면 재배차를 시도하고 없으면 idle이다. 예약은 배차 시점에 하며 대기 중 자원 점유가 없어 교착이 생기지 않는다. 기아(starvation)는 실제 거동이므로 KPI로 노출하고 aging 한도 옵션으로 완화한다. [첨부 A5]
10. 효과 시점을 고정한다. 저장 pick 완료 시 스테이션 이탈, place 시 셀 occupied·완료. 반출 pick 시 셀 empty, place 시 완료 후 즉시 또는 take-away 이벤트로 이탈. [첨부 A5]
11. 운전 모드는 SC-return(작업 후 home 복귀, tm1 대응), SC-stay(제자리 대기), DC(입고 1 + 출고 1 짝, 같은 존 작업만, 한쪽만 있으면 SC-stay 폴백, tm2 대응)로 명시 구분한다. 제자리 대기에서는 SC 저장 뒤 SC 반출이 사실상 DC가 되므로 세 모드를 섞어 보고하지 않는다. [첨부 A5]
12. 도착 모델 기본은 takt(3600/rate × (1 ± U·jitter))다. 소형 랙은 완충 동역학이 지배하므로 독립 Poisson 입·출고는 8 h 동안 재고가 크게 랜덤워크하여 비현실적일 수 있다. Poisson·CSV는 선택지로 유지하고 선택 근거를 기록한다. [첨부 A5]

사이클 간 이동시간과 handling 시간을 분리한다. 첨부의 SC/DC 관계식은 고정 동작 횟수의 정의를 확인하여 구현하고, 총 SC 시간에 이동시간만 더해 DC를 만드는 식으로 오용하지 않는다. [첨부 A1]

#### STC 프리셋과 검증 사례

| 프리셋 | 용도 | 취급 |
| --- | --- | --- |
| carBodyRev0 | 차체 저장 설비 Rev.0(5 bay×6 level, L 34.6 m, H≈12.67 m, E/A 4개소, 입고 68+22+3 JPH, 출고 92 JPH) | 프로젝트 사례. D14 확정 후 수치 고정. 범용 기본값 아님 |
| largeRackValidation | 50×15 단일 스테이션, random-empty, SC-return | 12.3절 5% 기준의 판정용. 소형 랙은 P1/P2 연속 가정과 5~8% 차이가 정상이며 정보용으로 라벨 |
| bozerWhiteSquare / B05 | Bozer & White 정방형·b=0.5 랙 | 셀 전수 열거 E[SC] = 1 + b²/3 검산(b=1 → 4/3, b=0.5 → 1.0833, 허용 1e-3) |
| generic20x10 | 중간 규모 일반 랙 | 성능·UI 기본 확인 |

감각 수치 앵커: Rev.0 조건에서 전장 주행 ≈19 s, 승강 12.67 m ≈17 s, 포크 사이클 ≈7 s, 평균 SC ≈49 s, 1 STC 이용률 252%(185 moves/h). 이 값은 검증 대조용이며 결과 판정 기준이 아니다. [첨부 A5]

#### STC DES 구현 순서

각 단계 종료 조건은 해당 테스트 통과다. [첨부 A5]

| 단계 | 작업 | 게이트 |
| --- | --- | --- |
| 0 | 폴더·커널 사본·three r128 확보와 SHA 기록, 템플릿 마커, 빌드·역추출 | hello 페이지 빌드 후 shipcheck에서 커널 테스트 통과 |
| 0′ | Rev.0 docx 본문 추출로 프리셋 수치·t_ü 의미·η 포함 여부 확정 | D14 확인표 갱신 |
| 1 | rng(목적별 스트림), kin(axisTime·posAt·makeMove·forkCycle) | posAt 종점·단조·연속, creep=0·delay=0이면 커널 travelTime과 일치 |
| 2 | fem9851 이식 + Python 골든 JSON | 1e-6 일치, Rev.0 % 재현 |
| 3 | validate·presets | as-is 분할이 "존 수지 불성립" 사유와 함께 검출 |
| 4 | engine 코어: 힙·도착·SC-return·단일 ST·통계 훅 | 결정성, 보존식, 스텝 실행 == 일괄 실행 비트동일, SC-return ≈ tm1E(대형 랙 5%), Bozer-White 열거 |
| 5 | 정책: SC-stay·DC·우선순위·저장/반출 규칙·다존·용량/블로킹·takt/pull·csv | 존 불변식, DC(E=A) ≈ tm2 5%, 기아·블로킹 카운트, warm-up 리셋 |
| 6 | stats 완성 | t 분위수, 집계 골든, 시간가중, overflow 빈, CSV 이스케이프 |
| 7 | 템플릿·앱(모델 탭, 단일 런)·차트(통계 탭) | 첫 브라우저 마일스톤(3D 없이 대시보드) |
| 8 | scene(렌더 모델)·view3d(애니메이션 탭) | 3D 검증 항목 |
| 9 | runner(Worker 풀·폴백)·실험 탭·내보내기 | Worker·폴백·애니메이션이 같은 seed에서 동일 KPI |
| 10 | optimizer·최적화 탭 | 열거 개수·유효성 기지, 스크리닝 완주 |
| 11 | 해석식 탭, README(검증 결과표), 최종 빌드 | 4.4절 검증 전부 |

규모는 JS 약 4.5k줄과 vendor 620 KB, 배포물 약 850 KB로 예상한다. 8 h 시나리오 1회는 약 1.5k 작업·15k 이벤트로 ms 단위에 끝나야 한다.

### 4.4 전용 검증·완료 조건

- 기존 11개 골든 사례를 실제 확보 후 재실행하고, creep·정위치·추가지연 비영 조건을 추가한다.
- SC 1회/실제 DC 1회에서 입고·출고·총 취급 카운터가 일치한다.
- 단면/양면, 비등간격 bay·level, I/O 높이 변경, 긴 거리·짧은 거리에서 독립 검산값과 일치한다.
- 초기 재고+입고−출고=종료 재고를 확인하고, 입출고 불균형에 따른 재고 변화가 보존된다.
- 유한 버퍼 정체가 다음 작업과 대기시간에 반영된다.
- 같은 수요·초기 상태·seed에서 화면 재생과 고속 실행의 결과가 일치한다. Worker 실행, 메인 스레드 폴백, 애니메이션 재생 세 경로의 KPI가 같다.
- 매 advanceTo마다 보존식(생성 = 완료 + 대기 + 진행)이 성립하고, 0.1 s 스텝 실행과 일괄 실행의 결과가 비트 동일하다.
- 저부하 변형(rate×0.3, SC-return)에서 시뮬 평균 SC와 tm1의 차이가 대형 랙 5% 이내, 소형 프리셋 약 8% 이내(정보용 라벨)다. DC(E=A)는 tm2 대비 5% 이내다.
- 정격 1 STC에서 busy가 약 100%로 수렴하고 물동량 ≈ 3600/tm1(약 70~75 moves/h), 입고 백로그 증가, saturated 플래그, 라인 정지시간 > 0이 관측된다. Rev.0 충족률(39.7%)과 같은 자릿수여야 한다.
- 수요 2배에서 saturated가 켜지고, takt jitter가 지정 범위 안에 있으며, 용량·백로그 회계가 맞는다.
- 셀 전수 열거 E[SC]가 Bozer & White 1 + b²/3와 1e-3 이내, 표본평균(2만 작업)이 열거평균과 2% 이내다.
- 최적화기 as-is 프리셋에서 1통로 내 모든 분할이 "존 수지 불성립" 사유로 infeasible, parallelAisles=3·SC-return이 실행가능(Rev.0 3대와 정합), parallelAisles=2 + DC nearest의 실행가능 여부가 순위표에 핵심 결과로 표시된다. addOutRight 변형에서 x2 분할 N=2가 도출된다.
- 성능: 20 시나리오 × 30 rep × 8 h가 Worker에서 1분 이내, 반복 실행 시 메모리가 안정적이다.

첨부의 특정 차체 프로젝트 값, 통로 수, 5 bay×6 level의 셀 수, 효율계수는 범용 기본값으로 승격하지 않는다. 예를 들어 입고 93/h·출고 92/h이면 순재고 증가가 생기므로 이를 정상상태 균형 사례로 사용하지 않는다. [첨부 A1]

### 4.5 후속 최적화

저장 위치·ABC 배치, DC 페어링, 대기 위치, 출고 우선순위 및 크레인 수를 탐색한다. 이동거리만 줄이는 정책이 출고 납기나 입고 대기를 악화시키는지 함께 평가한다. 다중 깊이·재배치·동일 레일 다중 크레인·고급 고장 모델은 후속 범위다.

## 5. 프로그램 2 — 직선 RGV

### 5.1 개발 목표

직선 레일의 스테이션별 물동량과 적재/공차 이동을 반영하여 사이클 시간, 차량 수, 이재부 병목을 산정한다.

### 5.2 필수 기능

| 구분 | 단독 v1.0 범위 |
| --- | --- |
| 레이아웃 | 레일 길이, 스테이션 좌표, 정지점, 이동 가능 구간 |
| 수요 | OD별 물동량, 시간대별 작업, 적재/공차 방향 |
| 장비 | 속도·가감속, 적재량, 상하차·정위치·인터록, 대기 위치 |
| 기본 모델 | 1대 왕복, 복수 독립 구간, 구간당 1대의 직렬 인계 |
| 운영·DES | FIFO·가까운 작업 기준 배차, 구간 인계 버퍼, 대기·차단 |
| 출력 | OD별 사이클, 공차율, 구간별 처리량, 목표 대비 부족량, 차량 구성안 |

같은 직선 레일의 복수 차량은 추월 가능 여부와 회피 공간에 따라 실행 가능성이 달라진다. **단독 v1.0은 1대 또는 물리적으로 분리된 구간을 정식 지원**하고, 동일 레일 다중 차량은 별도 공유레일 옵션이 검증되기 전 계산 가능으로 표시하지 않는다.

### 5.3 계산·개발 절차

1. 기존 HTML/JS에서 UI와 계산 코어를 분리하여 표준 플러그인의 첫 구현 사례로 활용한다.
2. 출발 위치→상차 위치의 공차 이동과 상차→하차 위치의 적재 이동을 구분한다.
3. OD별 작업 빈도와 운행 규칙에 따른 평균 시간을 계산한다. 복귀 운행이 없는데 왕복 거리를 자동 가산하지 않는다.
4. 구간 분할 운행은 각 구간의 처리능력뿐 아니라 인계 동작·버퍼·후단 대기를 계산한다.
5. 단일 차량의 명확한 작업 묶음은 분석식으로, 차량 간 인계는 DES로 확인한다.
6. 스테이션 배치는 위치(m)로 입력받아 거리행렬을 자동 산출하고, 구간별 속도제한(곡선·분기·저속구역)은 3.2절의 경계속도 연결 규칙으로 반영한다. 이재시간 T_L·T_U는 정지·위치결정·이재·출발을 포함한 실측치로 입력받되, 상세 지연 모드에서는 항목별로 분해할 수 있다. [첨부 A4]
7. 구간분할·구간당 1대 구성은 "Tandem"으로 명명하고, 블로킹이 없는 대신 환승(인계)이 발생함을 결과에 표시한다. 출력은 사이클타임 분해, 대차당 처리능력, 소요 대수(3.4절 fleet), 가동률 ρ, 병목 스테이션이다. [첨부 A4]
8. 보고서에는 "FEM 9.860 준용, 규격 적용대상 아님"과 안전규격 EN 619:2022를 고정 문구로 기재한다(D11).

단순 업무량 하한은 Σ(시간당 작업 수×작업당 자원 점유시간)으로 계산할 수 있다. 다만 최적 배차에 따라 공차시간이 달라지는 경우 고정 평균 시간을 모든 차량 수에 재사용하지 않는다.

### 5.4 전용 검증·완료 조건

- 기존 16개 시험 재현과 별개로 한 스테이션/같은 좌표/0건/한 방향 편중 사례를 추가한다.
- 공차 포함·제외 결과의 차이가 운행 이력과 일치한다.
- 직렬 인계에서 중간 버퍼가 가득 차면 상류 작업이 차단된다.
- 왕복 1대의 계산 결과를 수작업 타임라인과 대조한다.
- 공유레일 다중 차량 미지원 상태에서 사용자가 차량 수만 늘려 선형 처리량을 얻을 수 없도록 한다.

### 5.5 후속 최적화

대기 위치, 작업 선택, 구간 경계, 차량 배치, 인계 버퍼 크기를 비교한다. 동일 레일 복수 차량은 차량 길이·안전거리·추월/대피 가능 여부·예약 규칙을 갖춘 후 추가한다.

## 6. 프로그램 3 — Loop RGV

### 6.1 개발 목표

순환 레일의 다수 RGV가 스테이션 물동량을 처리할 때 차량 수 증가 효과, 정차·추종·합류·이재 대기 및 포화 구간을 검증한다.

### 6.2 필수 기능

| 구분 | 단독 v1.0 범위 |
| --- | --- |
| 레이아웃 | 단방향 단일 루프, 구간 길이·속도 제한, 곡선, 스테이션·정지/예약 블록 |
| 장비 | 차량 수·길이·적재량, 최소 간격, 가감속, 상하차·정위치 |
| 수요 | OD 물동량 표, 시간대 수요, 공차 재배치, 스테이션별 대기 |
| 분석 | 무간섭 기본능력, 1랩 법칙 개략 모드, 승인된 경험 보정 모드 η(N), 대수별 민감도, 물리 수용 한계 N_max |
| DES | 차량 위치·구간 점유, 추종·정차, 스테이션 점유, 블로킹, 배차 |
| 출력 | 대수별 처리량 곡선(정점 너머 포화 구간 포함), 공차율, 혼잡·이재·대기 시간, 병목 구간, 적정 대수, 목표/달성용량 여유 지표 |

복선, 이중 루프, 지름길, 추월, 복잡한 분기·합류는 토폴로지별 별도 확장으로 관리한다. 단순 루프에 없는 구조를 단순 보정계수로 지원한 것처럼 표현하지 않는다. 확장 후보 토폴로지는 단일 단방향 루프(v1.0), 이중 폐루프+숏컷, Tandem, SBSL, 복선 병렬이며, 토폴로지마다 평균거리와 간섭모델을 별도로 정의한다. [첨부 A4]

### 6.3 계산·개발 절차

1. Python 계열과 HTML/WCS 계열의 기능·입력·수식 비교표를 작성하고 기준본을 승인받는다.
2. 거리 단위, 한 바퀴 경계, 속도 구간의 연결, 정지 위치를 공통 커널로 통일한다.
3. 적재·공차 경로를 OD와 실제 배차로 산출한다.
4. “평균 적재 0.5바퀴+평균 공차 0.5바퀴=1바퀴”는 균일·독립 위치 등 특정 가정에서의 기준 사례로만 사용한다.
5. 대수별 계산은 차량 수를 단계적으로 바꾸어 처리량과 대기를 함께 관측한다. 레일·스테이션 공유가 있으므로 N배 처리량을 최종 결과로 사용하지 않는다.
6. 차량 길이와 간격으로 산정한 기하학적 수용 대수는 운전 가능한 처리능력 대수와 별도로 표시한다.
7. 혼잡을 DES에서 구현한 경우 별도 혼잡 효율계수를 다시 곱하지 않는다. 경험 보정 모드는 데이터 출처·적용 범위를 표시한다.
8. 경험 보정 모드의 혼잡 간섭계수는 η(N) = 1/(1 + β(N−1)) 또는 (1 − N/N_max)^γ 중 선택하고, β·γ는 사용자 입력 또는 실측 피팅값으로 노출한다. 처리능력을 대수에 선형 비례로 계산하는 경로는 두지 않는다(3.6절 #1). [첨부 A4]
9. 물리 수용 한계 N_max = ⌊L / s_min⌋(s_min = 차체길이 + 이격)을 계산하고, N > N_max는 오류, N > 0.3·N_max는 경고로 처리한다. 0.3은 실측 사례(루프 1.8 km에 76대 = 23.7 m/대, 실운용은 물리한계의 13~17%)에 근거한 내부 설계 기준이며 규격값이 아님을 표시한다(6.4절). [첨부 A4]
10. 1랩 법칙 E[적재거리] + E[공차거리] = L은 단방향 폐루프 균등분포 가정의 개략 산정 모드로 제공하고, 상세 모드는 실제 F_ij / D_ij 행렬을 사용한다. [첨부 A4]
11. 벤더 카탈로그 교차검증을 결과 검토 항목으로 둔다. 예를 들어 DAMBACH MONOFLEX 공표치(150 m/min, 0.7 m/s², 450건/h)를 1랩 법칙으로 역산하면 300 m 루프 가정에서 18대·16.7 m/대로 실측 벤치마크(23.7 m/대)보다 조밀하다. 카탈로그 수치가 공격적임을 사용자에게 알리고 설계 여유를 권고한다. [첨부 A4]

차량 수 증가 초기에 처리량이 단순 비례보다 커 보이는 현상은 공차거리 감소로 생길 수도 있다. 자동 오류 판정보다는 차량별 주행·대기 분해와 물리적 상한으로 설명 가능성을 검증한다.

### 6.4 전용 검증·완료 조건

- 첨부에 기재된 10,458 mm→11.83 s, 10,460 mm→21.51 s 불연속 사례를 재현 시험에 등록한다. 원인을 확인하기 전 수정 완료라고 표시하지 않는다.
- 0점 통과와 한 바퀴 거리 계산, 임계속도 전후, 짧은 속도 제한 구간의 시간이 연속적이다.
- 무부하·1대·저밀도·포화·특정 스테이션 집중의 단계별 사례를 갖춘다.
- 차량 간 금지 간격 침범, 정지 블록 중복 점유, 같은 화물 중복 수송이 발생하지 않는다.
- 대수 증가에 따라 처리량이 포화되는지, 포화 후 대기 증가가 결과에 드러나는지 확인한다. 기본 검토범위는 초선형 정점(N ≈ empty1/blockSize)을 넘어 포화가 화면에 반드시 나타나도록 자동 확장한다(D12).
- 차량 병목 시 utilization이 1.0으로 고정되는 기존 지표와 별도로 목표/달성용량 비율이 표시되고, 두 값이 다른 의미임이 도움말에 설명된다(D12).
- 기존 35건 회귀시험 중 결함 수정으로 기대값이 바뀌는 사례는 사전에 식별하고, 변경 사유를 테스트 주석으로 고정한다.
- 특정 점유율 경고값은 실측 또는 내부 설계 기준으로 표시하며 보편 규격값으로 표기하지 않는다.

### 6.5 후속 최적화

대기 차량 배치, 배차 우선순위, 스테이션 할당, 차량 수, 정차 정책을 비교한다. AI는 동일한 물리·예약 제약 안에서 작업 선택을 개선하도록 제한한다.

## 7. 프로그램 4 — 4-way Shuttle

### 7.1 개발 목표

수평 2방향 주행, 방향 전환, 교차로, 층별 배치, 리프트와의 인계를 반영하여 셔틀·리프트의 적정 조합과 목표 입출고 물동량을 산정한다.

### 7.2 필수 기능

| 구분 | 단독 v1.0 범위 |
| --- | --- |
| 레이아웃 | 층·행·열, 셀 간격, 주행 레일·교차로, I/O 및 리프트 위치 |
| 화물 | PLT/tote/box 유형별 사양 프리셋과 설비 적합성 검사 |
| 셔틀 | 수평 2축 속도·가감속, 방향 전환, 상하차, 적재·공차, 층별 대수 |
| 리프트 | 대수·속도·가감속·이재·출입 버퍼·작업 우선순위 |
| 기본 운용 | 층 고정 셔틀+tier-captive 화물 리프트, SC/DC, FIFO·가까운 작업 |
| DES | 실제 2방향 경로, 교차로·구간 예약, 셔틀 경합, 리프트 큐와 버퍼 |
| 출력 | 셔틀/리프트 능력 비교, 병목, 층별 부하, 대기, 목표 달성 조합 |

차량 자체가 층을 이동하는 tier-to-tier 방식은 필요한 리프트 동작과 차량 소유 상태가 다르므로 별도 기능 묶음으로 개발한다. 착수 시 해당 방식이 필수로 지정되면 일정에 추가 반영하고, 미지원 상태에서는 해당 프리셋을 활성화하지 않는다.

4-way v1.0은 단순 2-way 계산에 상수 시간을 추가하는 수준으로 완료 처리하지 않는다. 수평 2방향 이동과 방향 전환, 교차로 및 리프트 경합 검증을 필수로 한다. [첨부 A2]

### 7.3 셔틀 필요 대수 산식 정리

첨부 PRD에는 리프트·셔틀 처리량을 정의한 식과 필요 대수 식의 비율 방향이 일치하지 않는 부분이 있다. 다음은 원문 규격의 확정식이 아니라 **독립 자원 간 처리능력 균형을 위한 검산식**이다.

리프트 수 n_L, 리프트 사이클 시간 T_y, 사이클당 처리 화물 q_y, 셔틀 1대의 사이클 시간 T_x와 처리 화물 q_x를 같은 처리 기준으로 정의하면:

$$
C_{\mathrm{lift}}=\frac{3600n_Lq_y}{T_y},\qquad
C_{\mathrm{shuttle,1}}=\frac{3600q_x}{T_x}
$$

$$
N_{\mathrm{balance}}=
\left\lceil\frac{C_{\mathrm{lift}}}{C_{\mathrm{shuttle,1}}}\right\rceil
=
\left\lceil\frac{n_Lq_yT_x}{q_xT_y}\right\rceil
$$

q_x=q_y일 때는 ceil(n_L×T_x/T_y)로 정리된다. 단, 이 값은 독립 자원의 평균 능력을 맞추는 참고값이지 목표량을 반드시 달성하는 최적 대수가 아니다.

실제 설계에서는 목표량, 층별 편중, 리프트 공유, 교차로 경합, 버퍼, 가용도를 포함해 대수 조합을 탐색한다. 층 고정 방식은 전체 합계뿐 아니라 각 층의 셔틀 능력을 확인해야 한다. 셔틀이 리프트를 타는 방식에는 위 독립성 가정을 그대로 적용하지 않는다.

### 7.4 계산·개발 절차

1. 기존 HTML 입력·계산·시각화·시험을 분리하고 공통 스키마로 옮긴다.
2. 문서의 x/y/z 정의와 화면 좌표를 명시적으로 매핑한다. 특히 수직축을 y로 쓰는 기존 규격 표기와 렌더링 축을 혼용하지 않는다.
3. 수평 주행·방향 전환·리프트 이동·이재시간을 개별 검산한다.
4. 교차로·레일 점유와 리프트 입출구의 유한 버퍼를 구현한다.
5. 셔틀/리프트 대수 조합별로 동일 수요·초기 상태에서 시뮬레이션한다.
6. 부적합 조합을 먼저 제외한 후, 비용 또는 대수·대기시간 순으로 적합안을 정렬한다.
7. 기존 fourWayExtra 근사는 빠른 예비 계산 모드로 남길 수 있으나 상세 DES와 이름·가정을 구분한다.

### 7.5 전용 검증·완료 조건

- 기존 부속서 사례는 원문의 조건·기호·사이클당 화물 수까지 일치시켜 재검증한다.
- T_x 증가 시 독립 균형식의 필요 셔틀 수가 감소하는 등 방향성 오류가 없다.
- 한 층/다층, 셔틀 1대/다대, 리프트 1대/다대, 입출고 편중을 검증한다.
- 같은 교차로 동시 점유, 정면 충돌, 리프트 중복 탑승, 화물·셔틀 위치 불일치가 없다.
- 리프트 증설의 효과가 셔틀 또는 출입 버퍼 병목에서 제한되는 것을 확인한다.
- 교착 탐지와 진단 로그를 제공하고, 지원 모델에서 교착을 피하는 예약·해제 정책을 검증한다.
- 4-way 셔틀 DES는 STC DES(4.3절)의 계획 실행기·배차 시점 예약·존 검증·실행가능 판정 구조를 재사용하고, 층·교차로·리프트 자원만 추가한다. 설계공간은 층별 셔틀 수 × 리프트 수 × 우선순위 × 저장 규칙으로 정의하여 최적화기(11.1절 O1)에 연결한다.

### 7.6 후속 최적화

층별 셔틀 배치, 리프트 위치·대수, 작업 순서, 저장 위치, 경로·교차로 우선권을 비교한다. 차량 층간 이동, 다중 적재 리프트, 이중 깊이·재배치, 배터리·충전은 별도 확장한다.

## 8. 프로그램 5 — 컨베이어

### 8.1 개발 목표

직선·T형·십자형 및 네트워크 연결 컨베이어의 구간 능력, 이재 장치 병목, 존 점유, 버퍼 축적과 전체 흐름을 계산한다.

### 8.2 필수 기능

| 구분 | 단독 v1.0 범위 |
| --- | --- |
| 배치 형식 | 1자형, T형, 십자형, 기존 명세의 네트워크 연결형 |
| 구간 | 길이·속도, 연속 이송/정지 이송 구분, 화물 길이·간격·존 길이 |
| 이재 장치 | 스토퍼, 턴/트랜스퍼, 합류·분기, 별도 주기 동작 장치 |
| 운영 | 합류 우선순위, 방출 간격, 라우팅 비율, 전후단 인터록 |
| 버퍼 | 물리 용량, 초기 점유, 존 예약·점유, 만재 차단 |
| 출력 | 구간별 능력·headway, 병목, 대기/WIP, 포화시간, 개선 민감도 |

배치 형식과 벨트·롤러·체인 같은 기계 형식은 별도 속성으로 관리한다. 독립 동작 리프트가 포함되는 경우 자체 주기와 용량을 정의하고, 다른 모듈이 소유하는 리프트와 중복 생성하지 않는다.

### 8.3 계산 모델 구분

연속 이송 구간에서 속도 v가 m/min, 화물 피치 p가 m일 때 정상상태 상한은:

$$
Q_{\mathrm{pitch}}=\frac{60v}{p}
$$

여기서 p는 화물 길이+필요 간격이며 양수다. 실제 용량은 투입·배출 간격, 이재부 및 제어 조건에 의해 더 낮아질 수 있다.

정지·재출발 존, 이재 장치, 승강·턴 동작은 점유시간과 headway를 따로 계산한다. 여러 화물이 동시에 이동 가능한 컨베이어를 “전체 길이 이동 완료 후 다음 화물 출발”인 단일 서버로 모델링하지 않는다. 반대로 정지형 장치의 가감속을 무시하고 연속 피치식만 적용하지 않는다.

단순 버퍼에서 입력량 λ_in과 배출량 λ_out이 일정하고 λ_in>λ_out일 때:

$$
t_{\mathrm{full,min}}=
\frac{60B_{\mathrm{free}}}{\lambda_{\mathrm{in}}-\lambda_{\mathrm{out}}}
$$

B_free는 초기 점유를 제외한 남은 저장 수다. 입력≤배출이면 이 단순 정상 유량 조건에서는 유한 포화시간이 없다고 표시한다. 순간 버스트, 가변 화물 길이, 존 예약, 주기적 차단은 DES로 확인한다.

### 8.4 계산·개발 절차

1. cvcore·Excel·포팅 명세·골든 데이터의 실제 버전을 확보한다.
2. 계산식별 기준본과 의도된 차이를 승인받고, 기존 호환 모드와 신규 정밀 계산 모드를 구분한다.
3. 1자형→T형→십자형→네트워크형 순으로 단계 검증한다.
4. 구간·존·이재 장치를 고유 resourceId로 정의한다. 같은 이름이라는 이유만으로 동일 장치로 합치지 않는다.
5. 합류·분기·버퍼·공유 장치의 이벤트를 연결하고 질량수지를 확인한다.
6. 표·보고서의 중간 반올림과 표시 반올림을 구분한다. 기존 중간 올림/내림을 제거할 때는 결과 변화와 이유를 기록한다.
7. 기존 cvcore의 존·버퍼 규칙을 호환 모드로 유지한다. 구간 존 수 = INT(지점 간 거리 / 존 피치), 라인 버퍼 = Σ구간 존 수, 적체 초과 = φ − C, 버퍼 포화시간 = 구간 존 수 / 초과 × 60 [분], 물량 부족(starvation) 판정. 8.3절의 t_full,min과 같은 의미이며 두 식의 대응을 문서화한다. [첨부 A4]
8. 목표 부하율 4단계 판정을 제공한다. ≤ 목표(기본 85%) 적정, ~95% 주의, ~100% 위험, > 100% 처리불가. 경계값은 프로젝트 설정으로 바꿀 수 있고 기본값의 출처(사내 실무 기준)를 표시한다. [첨부 A4]
9. 병목 감도 분석을 기본 결과에 포함한다. 간선속도·존 피치·이재속도·가속도·지연·가동률을 하나씩 흔들어 처리량 변화를 순위화한다. 이 방식은 5종 공통 민감도 기능(9.4절)의 원형이다. [첨부 A4]
10. 공용 장치는 D13에 따라 호환 모드에서만 이름 기반 공유를 허용하고, 정밀 모드는 resourceId 참조를 요구한다.

### 8.5 전용 검증·완료 조건

- 기재된 2,617개 골든 사례와 6개 의도된 예외를 실제 파일 기준으로 재현한다. 의도된 예외도 사유·승인·기대값이 없는 한 자동 통과시키지 않는다.
- 속도·피치 기반 단순 사례를 수작업으로 검산한다.
- 초기 버퍼 공백/부분 점유/만재 상태와 입력≤배출/입력>배출을 검증한다.
- 분기 유량 합계와 합류 후 유량, 반복 방문하는 공유 이재 장치의 업무량이 보존된다.
- 연속 이송의 파이프라인 효과와 정지형 존의 차단 효과가 구분된다.
- 수식 포함 Excel 출력이 범위에 포함될 경우 화면과 동일 입력에서 재계산 결과를 대조한다.

### 8.6 후속 최적화

투입 간격, 합류 우선순위, 버퍼/존 수, 속도 설정, 병목 이재 장치 증설을 비교한다. 안전 인터록의 해제·우회는 최적화 후보에서 제외한다.

## 9. 소프트웨어 구조 및 공통 화면

### 9.1 계층별 책임

| 계층 | 책임 | 금지할 의존성 |
| --- | --- | --- |
| MotionKernel | 단위 변환, 운동·시간 계산, 공통 물리 검사 | DOM·UI·특정 장비 화면 참조 |
| SimulationCore | 이벤트 큐, 시계, 난수, 자원·버퍼 예약, 통계 | 애니메이션 프레임이 물리 시간을 결정 |
| DevicePlugin | 장비 입력, 분석 모델, 동작 계획, 정책·KPI | 다른 장비 플러그인 내부 함수 직접 호출 |
| AppShell | 입력 화면, 프로젝트, 결과 비교, 보고서, 오류 안내 | 화면 안에 별도의 계산식 복제 |
| IntegrationCore | 연결 그래프, 인계, 외부 주문·화물 추적 | 장비 처리량 합산을 라인 처리량으로 표시 |
| ExperimentRunner | 시나리오×반복 작업 전개, Worker 풀·폴백, 진행률·취소, 증분 집계, CI 계산 | 반복 간 seed 공유로 독립성 훼손, 작업 로그 전송 |
| Optimizer | 설계공간 열거, 유효성·해석식 사전필터, 2단계 스크리닝, 순위·Pareto | 실행 불가능한 동작 강제 또는 물리 상태 직접 수정 |
| RenderModel | 엔진 상태 → 위치·색·진행률·라벨·큐 스택의 순수 데이터 변환 | DOM·WebGL 직접 참조, 물리 시간 생성 |
| View2D / View3D | RenderModel을 Canvas 2D 또는 Three.js로 그리기, 카메라·피킹 | 엔진 상태 직접 수정 |

독립 개발 중에도 커널 원본은 하나를 기준으로 한다. 단일 HTML 배포를 위해 복사·내장할 수 있으나 버전·해시를 기록하고 자동 동기화 검사로 차이를 막는다. 첨부에서 언급한 5종 프로그램과 별도 STC DES의 복사본도 확인 대상에 포함한다. 통합판에서는 같은 커널을 한 인스턴스로 사용한다.

### 9.2 장비 플러그인 계약

| 기능 계약 | 입력/출력 및 역할 |
| --- | --- |
| capabilities | 지원 장비 형식·운영 모드·제약·지원 스키마 버전 |
| validateConfig | 설정 오류·단위·상호 모순·미지원 조합 검사 |
| calculate | 부작용 없는 분석 계산, 근거·가정·경고 반환 |
| createModel | 공통 시뮬레이션 환경에 장비·큐·자원 등록 |
| getPorts | 입력/출력 포트, 허용 화물, 용량, 인계 정보 |
| getPolicy / setPolicy | 승인된 정책 선택 및 파라미터 설정 |
| collectMetrics | 장비별 KPI를 공통 결과 규격으로 변환 |
| serialize / migrate | 저장·불러오기·구버전 변환 |
| buildReport | 공통 결과에서 장비별 상세 보고서 생성 |

함수명은 구현 시 조정할 수 있으나 책임과 데이터 계약은 공통 준비 단계에서 확정한다. 단독 화면과 통합 화면이 동일한 calculate/createModel 경로를 사용해야 한다.

### 9.3 실행·배포

- 개발: 모듈형 JavaScript, Node 기반 시험·빌드, 의존성 버전 고정.
- 기본 배포: 장비별 단일 HTML 5개; 통합 후 통합 HTML 1개를 추가 제공.
- 계산: 장시간 DES는 Worker에서 실행; file:// 환경에서 불가하면 분할 실행 fallback 제공. Worker 풀 크기는 hardwareConcurrency−1(최대 8), Blob Worker 생성 후 500 ms ping/pong으로 동작을 확인하고 실패 시 MainThreadSlicer 폴백으로 전환한다. 헤더에 `Worker: OK | fallback`을 표시한다. [첨부 A5]
- 시각화: 필수 2D 배치·상태·시간 그래프. 스태커크레인은 A5 설계의 3D 뷰(Three.js r128, InstancedMesh 셀·적재물, 크레인 그룹, 상태색 6종, 스프라이트 라벨, 자체 오빗 카메라, 셀 피킹)를 v1.0 범위로 구현한다. 나머지 4종은 같은 RenderModel을 소비하는 2D 뷰를 기본으로 하고 3D는 후속이다. 재생은 같은 엔진을 advanceTo(t)로 전진시키고 posAt으로 보간하며, rAF 기반 배속 1~1000×, 프레임당 dt 상한 0.1 s로 숨김 탭 복귀 시 시간 폭주를 막는다.
- 네트워크: 기본 계산·보고서에 외부 CDN·로그인·API 연결을 요구하지 않음. three.js는 vendor 사본과 SHA-256 대조로 인라인한다(1.3절).
- 저장: 명시적 JSON 내보내기를 기본으로 하고 브라우저 임시 저장만을 유일한 보관 수단으로 삼지 않음.
- 안정성: 실행 취소, 진행률, 잘못된 입력 안내, 메모리/이벤트 한도 초과 시 불완전 결과 표시.
- 시험: 소스 시험뿐 아니라 배포 HTML에서 추출한 실제 코드에도 같은 시험을 실행하는 shipcheck 적용. 모듈별 `<script class="engine" data-module="x.js">` 블록으로 인라인하고 속성 기반으로 역추출한다. 모듈 내 `</script>` 문자열 부재와 vendor 해시 일치를 빌드에서 단언한다. [첨부 A5]
- 저장 경로: Google Drive 동기화 폴더에서 dist·shipcheck 쓰기 중 EBUSY가 발생할 수 있으므로 쓰기 1회 재시도를 두고 산출물을 소형으로 유지한다. [첨부 A5]

### 9.4 공통 화면

| 화면 | 구성 |
| --- | --- |
| 프로젝트 | 장비 종류, 프로젝트·시나리오 선택, 저장/불러오기 |
| 모델 | JSON 정본 편집기 + 빠른 입력폼(JSON 패치), 프리셋 선택, 유효성 검사 결과와 사유 |
| 사양 입력 | 장비·레이아웃·화물·동작시간, 단위, 지연 간편/상세 모드, 보정계수와 근거 |
| 물동량·운영 | 입출고 수요, 시간대, 도착 유형, 초기 상태, 운전 정책 |
| 계산 결과 | 이론/보정/시뮬레이션 결과, 시간 분해, 목표 대비 부족·여유, 사이클타임·달성 물동량·포화 지표 동시 표시 |
| 애니메이션 | 재생/정지/리셋, 배속, 시계, 누적 입출고, 순간 물동량, 자원 상태색, 2D(3D는 STC) |
| 통계 | KPI 카드(입·출고 달성/요구, 자원별 busy, 평균·P95 대기, 최대 큐, 라인 정지시간, 판정 OK/NG+사유), 상태 파이/누적바, 시계열(warm-up 음영), 히스토그램 |
| 실험 | 설계변수 체크박스 → 시나리오 행렬, 반복 수 R, 진행률, 평균±CI 표, 2개 비교, CSV/JSON 내보내기, 결과행 → 애니메이션 재생 |
| 최적화 | 설계공간, 2단계 스크리닝, 순위표, Pareto 산점, 추천 카드, 실패 제약 설명 |
| 해석식 대조 | 분석식 결과 vs 시뮬 결과의 Δ%를 5/10% 색으로 표시, 차이 원인 분해 |
| 비교·검증 | 기준안/개선안, 입력 차이, seed·기간·가정, 검증 상태 |
| 보고서 | 결과 요약, 입력표, 계산 근거, 규격 준용 범위, VDI 3561 Bl.4 경고문, 경고·미지원·검증 범위 |
| 도움말 | 모델 가정, 정책 정의, KPI 정의, 단위·기호 |

탭 구성과 명칭은 A5의 7개 탭(모델·3D 애니메이션·통계·실험·최적화·해석식·도움말)을 기준으로 하고, 장비별 사양 입력은 모델 탭의 빠른 입력폼으로 구현한다. 구현 시 frontend-design·dataviz 지침을 적용한다. [첨부 A5]

### 9.5 시뮬레이션 구현 아키텍처

시뮬레이션은 계산 결과를 시각적으로 재생하는 기능이 아니라, 작업 도착·자원 점유·대기·차단·완료를 시간 순서로 실행하는 이산사건 모델로 구현한다. 장비별 독립 프로그램은 각 장비의 물리 동작과 자원 모델을 제공하고, 공통 SimulationCore가 이벤트 실행과 통계를 담당한다.

| 계층 | 구현 책임 | 주요 산출물 |
| --- | --- | --- |
| DemandGenerator | 일정·시간대·CSV 기반 작업 생성, 입출고·OD·seed 관리 | 재현 가능한 작업 스트림 |
| EventKernel | `(time, sequence)` 순서의 이벤트 큐, 시계, 취소·종료 조건 | 결정론적 이벤트 실행기 |
| ResourceModel | 장비·레일·리프트·스테이션·존·버퍼의 예약·점유·해제 | 자원 상태와 차단 전파 |
| DevicePlugin | STC/RGV/셔틀/컨베이어의 이동·이재·정책·제약 | 장비별 DES 모델 |
| Policy | FIFO, 가까운 작업, 고정 우선순위 등 승인된 작업 선택 | 설명 가능한 기준 정책 |
| Metrics | 완료량, 입·출고, 대기·차단·공차·WIP·이용률·P95/max, 시간가중 누적기, 고정빈 히스토그램(마지막 빈 = overflow), 시계열 빈, t 분위수 95% CI, CSV(UTF-8 BOM) | 공통 Result 구조 |
| Trace/Report | 이벤트 추적, 작업 타임라인, 병목·경고·가정 기록 | 재현 가능한 로그·보고서 |
| ExperimentRunner | 시나리오×반복 작업 전개, Worker 풀, 진행률·취소, 증분 집계(증분 == 일괄) | ScenarioResult |
| Optimizer | 설계공간 열거, 유효성·해석식 사전필터, 2단계 스크리닝(R=5 → 상위 k R=30), 사전식 순위, Pareto | OptimizerRow 목록, 추천 |
| RenderModel | 엔진 상태의 순수 데이터 스냅샷(위치·색·포크 진행·라벨·큐 스택) | 2D/3D 뷰 입력 |

구현 원칙은 다음과 같다.

1. 분석 계산과 DES는 같은 `MotionKernel`을 호출하되, 분석 보정계수와 DES의 고장·혼잡·대기는 한 손실을 두 번 적용하지 않도록 실행 모드와 적용 원장을 분리한다.
2. 이벤트 실행 순서는 시각과 증가하는 순번으로 고정한다. 동일 시각 이벤트의 우선순위는 작업 도착, 자원 해제, 자원 예약, 완료 등으로 계약에 기록한다.
3. 예약과 실제 점유, 이동 중 화물과 완료 화물을 별도 상태로 관리한다. 작업 완료는 장비별 취급량과 외부 출하량을 분리해 집계한다.
4. 정책은 장비 물리 모델을 직접 수정하지 않고 작업 선택·우선순위만 결정한다. 실행 전 제약 검사에서 레일 간격, 용량, 점유, 화물 적합성을 강제한다.
5. 동일 입력·초기 상태·정책·seed·실행 버전으로 이벤트 로그와 핵심 KPI를 재현한다. 시각화는 실행 결과를 소비하며 물리 시간의 원천이 되지 않는다.
6. 장시간 실행은 Worker에서 처리하고 진행률·취소·부분 결과·이벤트 수 제한을 제공한다. `file://` 환경에서도 기본 분석과 소형 DES가 동작하도록 번들 의존성을 유지한다.
7. 이벤트 큐는 이진힙 (t, seq)로 구현한다. 기본 이벤트 종류는 ARRIVAL(스테이션), RESOURCE_STEP(자원별 계획 실행), TAKEAWAY, SAMPLE, WARMUP_END, END이며 장비 플러그인이 종류를 추가할 수 있다. [첨부 A5]
8. 난수는 mulberry32 계열 seed 기반 생성기를 목적별 스트림(도착:스테이션, 셀 선택, take-away, jitter 등)으로 분리한다. 시나리오 간 같은 반복 번호는 같은 seed를 쓰는 공통난수(CRN)로 비교 분산을 줄인다. 테스트 중 `Math.random` 호출은 throw로 차단한다. [첨부 A5]
9. 시뮬레이션 실행 중 통계는 warm-up 종료 시 모든 누적기와 상태 시계를 리셋하되 엔티티 상태는 유지한다. 종료 시 후반¼/전반¼ 평균 큐 비율(queueTrend)과 saturated 플래그를 계산해 실행가능 판정에 쓴다. [첨부 A5]
10. 실행가능(feasible) 판정은 장비 공통 규칙으로 둔다: doneRatio ≥ doneRatioMin ∧ busy ≤ ρ_max ∧ queueTrend < 1.5 ∧ unservable = 0 ∧ maxQueue ≤ 목표. 임계값은 Scenario.targets에 저장한다. 최적화기의 사전식 목적은 자원 수 오름차순 → 실행가능 → 총 자원 수(×병렬 통로) → 평균 대기 오름차순이다. [첨부 A5]
11. 애니메이션은 별도 엔진이 아니라 같은 엔진 인스턴스를 advanceTo(t)로 전진시키고 RenderModel을 거쳐 그린다. 시각화를 끄고 실행한 결과와 재생 결과의 KPI가 같아야 한다(12.2절 S05).
12. 렌더 모델은 순수 함수로 두어 Node 테스트에서 위치↔기하, 상태→색, 포크 진행 0..1을 검증한다. [첨부 A5]

### 9.6 시뮬레이션 출시 단계

| 단계 | 범위 | 출시 조건 |
| --- | --- | --- |
| S0 공통 엔진 | 이벤트 큐, 시계, 자원, 통계(누적기·히스토그램·시계열·CI), seed·CRN, 로그, RenderModel 계약 | 독립 검산·동일 실행 재현, 통계 골든값 |
| S1 장비 단위 DES | 5종 각각의 작업·자원·버퍼·운영 정책, 애니메이션 재생(2D, STC는 3D), 통계 대시보드, 해석식 대조 탭 | 장비별 질량수지·점유·대기 시험 통과, 재생·고속 실행 KPI 동일 |
| S2 시나리오 실험 | 실험기: 수요·대수·버퍼·정책·속도 조합 반복, Worker 풀, CI, 2개 비교, CSV/JSON | 기준안/대안 동일 조건 비교와 결과 저장, CI 폭 ∝ 1/√R |
| S2′ 설계 탐색기 | 최적화기: 설계공간 열거, 유효성·해석식 사전필터, 2단계 스크리닝, 순위·Pareto·추천 | 열거 개수·무효 사유 기지, 스크리닝 완주, 기준행 고정 표시 |
| S3 연결 라인 DES | 포트·화물 소유권·공유 자원·차단·교착 | 대표 라인 인계·질량수지·복구 시험 통과 |
| S4 최적화 연계 | OR-Tools·휴리스틱·AI 정책을 제한된 인터페이스로 연결 | 기준 정책 대비 제약 위반 없음과 재현 가능한 개선 |

S0~S2는 장비별 독립 프로그램의 필수 구현 범위다. S2′ 설계 탐색기는 공통 모듈로 S0에서 함께 만들고, 장비별 설계공간 정의가 승인된 장비(STC 우선)부터 활성화한다. S3는 5종 독립판 승인 후 통합 2차에서 구현하며, S4는 시뮬레이션 기준선이 승인된 뒤 선택적으로 진행한다. STC는 A5의 설계를 S0~S2′의 첫 구현 사례로 삼고, 여기서 추출한 공통 모듈(rng·stats·runner·optimizer·scene)을 다른 4종이 재사용한다.

## 10. 5개 프로그램 완료 후 통합 계획

### 10.1 통합 착수 원칙

5종이 각각 단독 승인 기준을 통과한 뒤 통합판 개발을 시작한다. 그 전에 공통 규격·인터페이스를 설계하고 가짜 연결 대상과 계약 시험을 하는 것은 허용하되, 미완성 장비를 통합 화면에 넣어 완료한 것으로 취급하지 않는다.

### 10.2 통합 1차 — 단일 실행 프로그램

1. 5종 프로그램의 승인 버전과 입력 스키마를 고정한다.
2. 공통 화면·프로젝트 관리·단위 설정·보고서 기능을 AppShell로 모은다.
3. 동일 장비를 프로젝트 안에 여러 개 등록할 수 있도록 moduleId를 부여한다.
4. 개별 JSON을 통합 프로젝트로 가져오고 다시 단독 프로그램에서 읽을 수 있는 내보내기를 제공한다.
5. 동일 입력의 단독판/통합판 분석 결과와 고정 seed DES 결과가 일치하는지 회귀시험한다.
6. 통합 실행 파일을 배포하되 독립 프로그램도 유지하여 장비별 검증과 현장 활용을 지원한다.

이 단계는 한 화면에서 5종을 계산·비교하는 제품이다. 장비를 이어 놓았다는 이유만으로 연결 시스템 처리량을 계산했다고 표시하지 않는다.

### 10.3 통합 2차 — 장비 연결·전체 라인 검증

대상 예시는 “입고 컨베이어→직선/Loop RGV→STC 또는 4-way Shuttle→출고 컨베이어”이다. 5종이 모두 반드시 직렬 연결되는 것은 아니며 프로젝트별 필요한 장비와 분기·합류를 선택한다.

| 연결 항목 | 정의 |
| --- | --- |
| 연결 그래프 | 장비 인스턴스의 입력/출력 포트와 버퍼 연결 |
| 전역 시간 | 공통 시계·이벤트 큐 사용; 독립 엔진의 시간만 맞추는 임의 결합 금지 |
| 인계 규칙 | 준비 확인→수용 능력 예약→물리 이송→인수 확인→원위치 점유 해제 |
| 화물 소유권 | 물리 위치·운송 중 상태를 하나의 소유자만 기록; 예약과 실제 점유 구분 |
| 이재시간 | 상·하류 중 어느 쪽이 모델링하는지 계약에 명시하여 이중 가산 방지 |
| 공유 자원 | 리프트·스테이션·컨베이어를 고유 ID로 한 번만 생성하고 작업별로 참조 |
| 차단·교착 | 유한 버퍼, 인터록, 예약 순서, 해제·타임아웃·진단 정책 |
| 외부 수요 | 외부 작업 ID를 유지하고 장비별 내부 작업 ID와 매핑 |

1개 화물이 5개 장비를 거쳐도 출하 완료는 1개다. 장비별 취급량과 전체 시스템 출하량을 별도로 집계한다.

단순 직렬 연결이고 모든 장비가 동일 단위로 화물당 1회 처리한다면 min(장비별 처리능력)은 시스템의 분석 상한으로 활용할 수 있다. 분기·합류·재순환·공유 장비에서는 화물당 방문 횟수와 경로 비율을 반영한 자원 부하를 먼저 계산하고 DES로 검증한다. 상한값을 실제 달성 처리량으로 표시하지 않는다.

### 10.4 통합 검증 시나리오

- 컨베이어→직선 RGV→STC: 기본 인계, 출고 완료량과 화물 보존.
- Loop RGV→복수 STC: 특정 통로 집중, 스테이션 공유, 루프 정체.
- 컨베이어→4-way Shuttle: 리프트 병목, 층별 부하, 입출고 버퍼.
- 분기·합류와 공유 이재 장치: 자원 중복 생성 및 이중 완료 방지.
- 상류 과잉 투입·하류 정지·버퍼 만재: 차단 전파와 복구 시간.
- 종료 시 운송 중인 화물: WIP·재고·완료량을 빠짐없이 설명.

통합판의 합격은 개별 프로그램 결과 보존과 연결 모델의 질량수지·인계·공유자원 시험을 모두 통과하는 것으로 정의한다.

## 11. 최적화·AI 적용 계획과 공개 코드 활용

### 11.1 적용 순서

| 단계 | 내용 | 도입 조건 |
| --- | --- | --- |
| O0: 기준 정책 | FIFO, 가까운 작업, 고정 우선순위 등 설명 가능한 운영안 | 단독 v1.0에 필수 포함 |
| O1: 설계 탐색 | 차량·리프트·버퍼 수, 속도, 작업 규칙의 격자/구간 탐색 | 검증된 계산·DES와 실험 기록 |
| O2: 수리·휴리스틱 최적화 | 작업 순서, 자원 배정, 저장 위치의 제약 기반 탐색 | 제약식·목적함수·시간 한도 확정 |
| O3: 학습 기반 최적화 | 대체 모델, 강화학습 배차·저장 정책 등 | 충분한 학습/검증 사례, 기준안 대비 재현 가능한 개선 |
| O4: 운영 연계 | 실제 데이터 수집·추천안 전달 | 별도 보안·인터페이스·운영 승인 프로젝트 |

O1·O2는 최적화이지만 반드시 AI 학습은 아니다. 초기부터 강화학습을 필수로 도입하지 않는다. 물리 계산의 정답을 언어모델이 대신 생성하게 하지 않는다.

O1 설계 탐색은 9.6절 S2′ 최적화기로 구현한다. 절차는 다음과 같다. [첨부 A5]

1. 설계공간을 변수별 후보 집합의 곱으로 열거한다(예: 존 분할 × 스테이션 변형 × 운전 모드 × 페어링 × 우선순위 × 저장 규칙 × 병렬 통로 수).
2. 유효성 검사(validateConfig)로 무효 설계를 사유와 함께 제외한다.
3. 해석식 사전필터로 부하율 상한을 계산해 명백히 불가능한 설계를 건너뛴다. 필터값은 행에 남긴다.
4. 1단계 스크리닝을 반복 5회로 전체 실행하고, 상위 k개를 2단계에서 반복 30회로 재실행한다.
5. 실행가능 판정(9.5절 원칙 10)을 통과한 설계만 순위에 들어가며, 사전식 목적으로 정렬한다.
6. 총 자원 수 대 평균 대기의 Pareto 집합을 산점도로 표시하고 추천 카드를 만든다. 기준 설계(예: Rev.0 상당)는 순위표에 고정 행으로 표시한다.
7. 실행 불가능한 설계는 "하위 N개 실패 제약"을 요약해 어떤 제약이 주로 깨졌는지 보여준다.

비용 데이터가 없으면 자원 수와 대기시간만으로 순위를 매기고 "최소 비용"이라 부르지 않는다(11.2절).

AI 코딩 보조는 시험 작성·입력 변환·문서화에 활용할 수 있으나, 생성된 수식과 코드도 동일한 검산·회귀시험·리뷰를 통과해야 한다. 현장 원시 데이터나 비공개 소스를 외부 AI 서비스에 보내는 기능은 별도 승인 없이 기본 활성화하지 않는다.

### 11.2 장비별 최적화 대상

| 장비 | 조정 변수 | 필수 평가 지표 |
| --- | --- | --- |
| 스태커크레인 | 저장 위치, DC 페어링, 대기 위치, 통로 수 | 입출고 달성, P95 대기, 공차 이동, 필요 설비 수 |
| 직선 RGV | 배차·대기 위치, 구간 분할, 인계 버퍼 | 처리량, 공차율, 인계 대기, 실행 가능성 |
| Loop RGV | 차량 수, 배차, 대기 분산, 스테이션 할당 | 처리량, 정체, 차량 간격, 대기·공차 |
| 4-way Shuttle | 층별 대수, 리프트, 저장 위치, 작업·교차로 순서 | 층별 달성, 리프트 대기, 경합, 교착 |
| 컨베이어 | 방출 간격, 합류 우선순위, 존/버퍼 수 | 출하량, WIP, 최대 대기, 상류 차단 |
| 통합 라인 | 장비 조합, 버퍼 위치, 공정별 우선순위 | 전체 출하, 납기, 총 WIP, 투자·운영비 |

비용 데이터가 없으면 “최소 비용 최적안”이라고 표시하지 않는다. 적합한 설계 중 최소 대수, 대기시간, 처리량 여유 등 공개된 기준으로 후보를 제시한다.

### 11.3 AI 검증·안전 제약

- 상태·가능 행동·보상·종료 조건을 시뮬레이션 엔진과 분리된 정책 인터페이스로 정의한다.
- 차량 간격, 점유, 리프트 용량, 화물 적합성은 보상 페널티에만 맡기지 않고 실행 전 강제 검사한다.
- 자유 입력 JavaScript나 AI 생성 문자열을 즉시 실행하지 않는다. 승인된 정책·모델과 제한된 입력 규격을 사용한다.
- 동일한 수요·초기 상태·외생 난수 조건으로 기준안과 개선안을 비교한다.
- 학습에 쓰지 않은 기간·레이아웃·피크 패턴을 검증용으로 분리하고 미래 주문 정보를 정당한 입력 없이 사용하지 않는다.
- 평균만 아니라 신뢰구간, 최악 시나리오, 미완료 작업, 납기·대기 악화도 보고한다.
- 성능이 불명확하거나 모델 적용 범위를 벗어나면 기준 정책으로 복귀한다.
- 초기 제품은 설계 검토용이다. 실제 PLC/WCS 제어 명령을 직접 출력하는 기능은 범위에서 제외한다.

### 11.4 공개 자료의 활용 범위

다음은 공개 저장소·공식 문서를 확인한 참고 후보다. 본 제품과 장비 구조가 다르므로 프로젝트 전체를 그대로 가져오는 계획이 아니라 필요한 구조·알고리즘·시험 방법을 선별하는 계획이다. 출처별 내용은 자료 설명과 본 계획의 적용 판단을 구분한다.

| ID / 공식 자료 | 확인한 성격 | 본 계획의 활용 판단 | 적용 한계 |
| --- | --- | --- | --- |
| R1 [SLAPStack](https://github.com/malerinc/slapstack) | 블록 적재 창고 시뮬레이션, 상태·이벤트·정책 분리, 저장 위치/화물 선택 실험 | 저장·배차 정책 API와 반복 실험 구성 참고 | 랙식 STC·4-way의 운동 산식 검증 기준이 아님 |
| R2 [RAWSim-O](https://github.com/merschformann/RAWSim-O) | RMFS 대상 이산사건 시뮬레이션과 확장형 의사결정 구조 | 다수 이동체·작업·시각화 분리 구조 참고 | 원 설비 유형이 다르며 GPL-3.0 및 의존성 검토 필요 |
| R3 [TA-RWARE](https://github.com/uoe-agents/task-assignment-robotic-warehouse) | 작업 할당용 다중 로봇 환경, A* 경로와 기준 휴리스틱 | 배차 기준안·상태/행동 설계·비교 실험 참고 | 지표가 order-lines/h이며 PLT/h로 직접 전환 불가 |
| R4 [RWARE](https://github.com/semitable/robotic-warehouse) | 격자 기반 협동 강화학습 환경 | 작은 정책 검증 실험과 행동 제약 설계 참고 | 격자 스텝을 실제 가감속 기반 설비 시간으로 간주하지 않음 |
| R5 [DynStack](https://github.com/dynstack/dynstack) | 불확실한 환경의 동적 적재 최적화 벤치마크 | 시뮬레이터와 의사결정 알고리즘의 분리 참고 | 본 STC 사이클 계산기를 대체하지 않음 |
| R6 [SA-Tabu](https://github.com/ShengranHu/SA-Tabu) | 그룹 제약이 있는 저장 위치 할당의 대체모델 보조 탐색 | 저장 위치 최적화 후보 알고리즘 비교 | 입출고·물리·운영 제약은 본 모델에 맞게 재정의 |
| R7 [Google OR-Tools](https://developers.google.com/optimization) | 라우팅·제약·스케줄링 등의 최적화 도구 | 선택적 오프라인 작업 순서·배정 비교 실험 | 자체가 AI 학습 모델이나 충돌 회피 DES는 아님 |
| R8 [SimPy 공식 문서](https://simpy.readthedocs.io/en/latest/) | 프로세스 기반 이산사건 시뮬레이션 프레임워크 | 소형 독립 검산 모델·오프라인 실험 참고 | 기본 제품을 Python/Streamlit로 전환한다는 의미 아님 |
| R9 [Waremax](https://github.com/Skelf-Research/waremax) | 이동·혼잡·스테이션 대기를 포함한 창고 시뮬레이션과 Gymnasium 연계 | 시간 분해 지표, 재현 가능한 정책 실험, 학습 인터페이스 참고 | 기능 성숙도·실행 재현성·라이선스 확인 전 제품 코드 채택 금지 |
| R10 [JaamSim](https://github.com/jaamsim/jaamsim) | 범용 이산사건 시뮬레이션과 자원·큐·시각화 | 자원·큐·물류 흐름의 독립 검산 모델 참고 | 5종 장비의 물리 산식이나 제품 실행 엔진을 대체하지 않음 |
| R11 [OFACT](https://github.com/OpenFactoryTwin/ofact) 및 [Open-RMF Demos](https://github.com/open-rmf/rmf_demos) | 디지털 트윈 데이터 모델·다중 로봇/인프라 연계 사례 | 포트·상태·외부 인터페이스 설계 참고 | 실제 WCS·PLC 연동과 현장 제어는 범위 밖 |

외부 코드 채택 전에는 대상 커밋, LICENSE, NOTICE, 의존성, 데이터 사용 조건을 기록한다. 코드 라이선스와 포함된 현장 데이터의 사용 권한을 같은 것으로 간주하지 않는다. 라이선스가 불명확하거나 결과 재현이 되지 않는 예제는 참고 목록에만 두고 계산 기준선으로 채택하지 않는다.

## 12. 계산 검증·시뮬레이션 검증 계획

### 12.1 검증의 세 층

| 층 | 검증 질문 | 방법 |
| --- | --- | --- |
| 수식·구현 검증 | 정해진 수식과 조건을 코드가 맞게 계산하는가? | 손계산, 독립 구현, 경계·속성 시험, 기존 골든 사례 |
| 모델 검증 | 의도한 자원·작업·물류 흐름이 맞게 표현되는가? | 이벤트 추적, 타임라인, 질량수지, 극단·장애 사례 |
| 현장 타당성 검증 | 실제 설비와 비슷한 시간·처리량·대기를 예측하는가? | 제조사 사양, FAT/SAT·운영 로그, 별도 검증 기간 비교 |

계산기와 DES가 같은 커널을 쓰면 서로 결과가 같아도 같은 오류를 공유할 수 있다. 따라서 동일 커널 간 비교만으로 검증을 끝내지 않고 독립 검산 및 실측 대조를 포함한다.

### 12.2 공통 시험 목록

| 시험 ID | 시험 | 합격 원칙 |
| --- | --- | --- |
| K01 | mm/m, m/min/m/s 변환 | 왕복 변환·단위 변경 시 같은 물리 결과 |
| K02 | 0거리, 짧은 거리, 임계거리, 긴 거리 | 정의된 경계값과 연속성 충족 |
| K03 | a≠d, 적재/공차, creep·지연·정위치 | 동작별 적용 횟수 및 합계 일치 |
| K04 | 연속 속도 구간 분할 | 실제 제약이 같으면 임의 분할로 시간 불변 |
| K05 | SC/DC·다중 적재·수요 단위 | cycle·화물·입고·출고 카운터 정확 |
| K06 | 가용도·혼잡·피크 | 중복 보정과 미적용 입력 없음 |
| K07 | s = s_crit 경계, creep on/off 경계, jerk 방식 ⓐ/ⓑ | 좌·우 극한 차이가 12.3절 결정론 오차 이내, a = d에서 FEM 9.860 식(3)과 일치 |
| K08 | 대수산정 골든: OPIL 예제1 T_C = 2.912 min·AT = 14.7 min/h·AN = 2.025 → 3대, OPIL 예제2 WL = 39.76 min/h·AN = 0.676 → 1대 | 값 일치, 보정계수 1.0 시 대수 변화가 경고와 함께 표시 |
| K09 | 물리 정합성 N > N_max, N > 0.3·N_max | 오류·경고가 각각 발생 |
| K10 | 지연 간편/상세 모드 전환 | 기본값에서 결과 불변, 항목별 적용 횟수 합계 일치 |
| K11 | posAt 보간 | posAt(0) = 0, posAt(T) = s, 단조, 구간 경계 속도 연속, 빠른 축 정지 유지 |
| S01 | 빈 시스템·0건·단일 작업·과부하 | 예외·상태·완료/미완료가 설명 가능 |
| S02 | 재고·WIP·화물 소유권 | 손실·중복·음수 재고 없음 |
| S03 | 자원·레일·교차로 예약 | 금지된 동시 점유 없음 |
| S04 | 동일 seed·수요·정책 반복 | 동일 지원 환경에서 결과 재현, 테스트 중 `Math.random` 호출 시 실패 |
| S05 | Worker/분할 실행/시각화 on·off/애니메이션 재생 | 완료 수·핵심 KPI 동일 |
| S06 | warm-up·측정 종료·진행 중 작업 | 통계 경계와 미완료 처리 일관, warm-up 리셋 후 엔티티 유지 |
| S07 | 매 advanceTo 보존 불변식 | 생성 = 완료 + 대기 + 진행, 자원 상태시간 합 = 측정시간 |
| S08 | 0.1 s 스텝 실행 vs 일괄 실행 | 비트 동일 |
| S09 | 해석식 대조: SC-return ≈ tm1E, DC(E=A) ≈ tm2(대형 랙) | 5% 이내, 소형 랙은 라벨 후 8% 이내 |
| S10 | Bozer & White 셀 전수 열거 | 1 + b²/3 대비 1e-3, 표본평균 → 열거평균 2% |
| S11 | 수요 2배·용량 초과·take-away 제한 | saturated 플래그, 백로그·차단시간 회계 일치 |
| S12 | 통계 모듈 | t(9) = 2.262, t(29) = 2.045, 시간가중 평균, overflow 빈, CSV 이스케이프·BOM |
| E01 | 실험기 반복 수 R 10 → 40 | CI 폭 ≈ 1/√R, 증분 집계 == 일괄 집계 |
| E02 | 실험 CSV 내보내기 | 한글 헤더가 Excel에서 정상(UTF-8 BOM) |
| P01 | JSON 저장·재입력·구버전 변환 | 주요 입력·단위·결과 보존, major.minor 경고 |
| P02 | 소스/단독 배포/통합 배포 | 동일 입력의 결과 동일 |
| P03 | 오프라인·잘못된 파일·실행 취소 | 네트워크 없이 동작, 명확한 오류·취소, "다른 이름으로 저장(HTML만)" 사본도 동작 |
| P04 | 3D 뷰(STC) | 콘솔 오류 0, 랙 피치·스테이션 위치·home 시작 정확, 가감속·동시 이동 가시, 상태색 6종, 배속, 오빗/팬/줌/리사이즈, 탭 숨김 복귀 시 시간 폭주 없음, 셀 클릭 정보 |
| I01 | 장비 간 인계·공유 자원 | 시간·용량·소유권을 한 번만 반영 |
| I02 | 분기·합류·재순환 | 외부 화물 기준 처리량·질량수지 일치 |
| O01 | 기준안/개선안 비교 | 동일 외생 조건, 제약 위반안 제외 |
| O02 | 최적화기 | 열거 크기·무효 사유·순위·Pareto 소속 기지값 일치, 스크리닝 완주 |
| V01 | 벤더 카탈로그 교차검증 | 카탈로그 공표치 역산이 타당성 범위 밖이면 경고 |

### 12.3 오차·통계 합격 기준 제안

다음 수치는 프로젝트 내부 검수 기준의 초기 제안이며 FEM·VDI가 공통으로 요구하는 값이라고 주장하지 않는다.

| 대상 | 초기 제안 | 확정 방법 |
| --- | --- | --- |
| 결정론적 운동시간 | 절대 오차 ≤ max(10⁻⁶ s, 기준시간의 절댓값×10⁻⁸) | 골든값 정밀도·수치 구현에 맞춰 G1에서 승인 |
| 카운터·상태 | 정수 수량·점유·소유권은 정확 일치 | 모든 회귀시험에서 강제 |
| 같은 가정의 분석식/독립 시뮬레이션 사이클타임 | 차이 5% 이내를 초기 검토선으로 사용(사내 실무 기준) | 표본 오차·경계·분포·정책 차이를 먼저 통제. 대형 랙 프리셋에서 판정하고 소형 랙은 정보용 |
| 분석식/시뮬레이션 N대 물동량 | 차이 10% 이내(사내 실무 기준) | 혼잡·포화 구간은 차이 원인 분해 후 판정 |
| 초기 실측 처리량 대조 | ±10%를 파일럿 검토선으로 사용 | 현장·계약별 허용값과 데이터 품질로 별도 확정 |
| 실측 사이클타임 대조 | 편차 ≤ 5%, 실측이 더 빠른 것은 무방, 5회 수행 평균 | VDI 3561 Bl.4·FEM 9.860 §6 절차를 따르되 원문 확인 전 "참고" 표시(D02) |
| 대기·P95·최대 큐 | 목표 서비스 수준과 합의한 오차 적용 | 평균 처리량 기준으로 대기 오차를 대신 판정하지 않음 |
| 반복 실험 | 우선 30개 seed, 95% 신뢰구간 | 구간 폭이 넓으면 추가 반복·기간 확대 |

대표점 근사와 실제 셀 분포 DES처럼 가정이 다른 두 모델에 5% 기준을 무조건 적용하지 않는다. 설명 가능한 차이는 모델 가정의 차이로 보고하고, 동일 조건의 구현 오류와 구분한다. ±10% 이내여도 잘못된 원인을 상쇄하여 맞춘 모델은 승인하지 않는다.

### 12.4 시뮬레이션 실험 규칙

- 이벤트 순서는 (시각, 순번)으로 결정하여 동시 이벤트도 재현 가능하게 한다.
- 도착 수요, 초기 재고, 정책 탐색 난수 스트림을 분리한다. 정책에 따라 난수 호출 수가 바뀌어 다른 주문을 비교하는 오류를 방지한다.
- 8시간 실제 교대 재현 같은 종료형 실험은 정해진 초기 상태부터 측정한다. 임의 warm-up으로 실제 피크를 제거하지 않는다.
- 정상상태 실험은 초기 과도구간을 검토해 warm-up을 정하고 상태는 유지한 채 통계만 초기화한다.
- 완료량은 측정 구간에 실제 완료된 건수를 센다. 체류시간은 대상 작업 집단과 관측 경계를 명시하고 종료 시 미완료 건수를 함께 보고한다.
- 주행·이재·대기·차단·고장 시간은 중복 집계 여부를 명시한다. 자원 상태 합계가 측정시간과 맞는지 검사한다.
- 결과마다 입력 해시·버전·seed·기간·반복 수·가정·적용 정책을 기록한다.
- 현장 보정용 데이터와 최종 검증용 데이터를 분리하여 보정값에 맞춘 결과만 제출하지 않는다.

### 12.5 실행 성능·사용성 목표

착수 시 기준 PC·브라우저·표준 레이아웃·주문 수를 기록한 벤치마크를 고정한다. 초기 목표는 기본 분석 입력 변경 후 1초 내 결과 표시, 실행 중 취소 요청 2초 내 응답으로 제안한다. 대규모 DC 전수 조합이나 장시간 DES는 별도 성능 등급과 진행률을 제공한다.

DES의 완료 시간은 이벤트 수와 자원 규모에 크게 좌우되므로 장비 수·주문 수 없이 “모든 시나리오 몇 초”를 약속하지 않는다. G1에서 소형/대표/대형 기준 사례와 실행시간·메모리 상한을 승인한다.

STC의 초기 벤치마크는 A5를 따른다. 8 h 시나리오 1회(약 1.5k 작업·15k 이벤트)는 ms 단위, 20 시나리오 × 30 rep × 8 h는 Worker 풀에서 1분 이내, 반복 실행 시 메모리 안정. Worker는 집계값만 반환하고 메인 스레드는 시나리오 집계 후 히스토그램·시계열 원본을 폐기한다. 다른 4종은 같은 형식으로 장비별 기준 사례를 G1에서 정한다. [첨부 A5]

### 12.6 시뮬레이션 구현 합격 기준

장비 단위 DES는 다음 조건을 모두 만족해야 독립 프로그램의 필수 기능으로 승인한다.

- 0건, 단일 작업, 수요 과부하, 초기 버퍼 부분 점유, 버퍼 만재를 실행하고 완료·미완료·대기 상태를 설명한다.
- 작업 ID·화물 ID·자원 ID의 생성, 예약, 점유, 해제, 완료가 이벤트 로그에서 추적된다. 중복 완료·유실·음수 재고가 없어야 한다.
- 자원별 주행·이재·대기·차단·고장 시간이 서로 중복되지 않고 측정시간과 합계가 검산된다.
- 유한 버퍼가 가득 찬 경우 상류 차단이 발생하고, 하류가 재개되면 정해진 정책에 따라 회복된다.
- 동일한 입력·초기 상태·정책·seed로 재실행한 결과의 완료량·WIP·대기·자원 점유와 정규화 로그가 일치한다.
- 분석 모드의 공칭/보정 처리량과 DES의 완료량을 구분하여 표시하고, 차이를 작업 도착·자원 경합·버퍼·정책·종료 경계로 설명한다.
- 실험기가 시나리오 행렬을 반복 실행하고 평균±95% CI를 표로 내며, Worker·폴백·애니메이션 세 경로의 KPI가 같은 seed에서 동일하다.
- 통계 대시보드의 상태 파이 합이 100%이고, 실행가능 사례에서 warm-up 이후 물동량이 평탄하며, KPI 카드 값이 표 값과 같다.
- 해석식 대조 탭에서 분석식과 시뮬 결과의 Δ%가 표시되고 5/10% 기준으로 색이 바뀐다.
- 설계 탐색기를 활성화한 장비는 열거·유효성·스크리닝·순위·Pareto가 12.2절 O02를 통과한다.
- S3 연결 라인은 대표 시나리오에서 포트 인계, 화물 소유권, 공유 자원, 차단·교착, 종료 시 운송 중 WIP를 검증한다.

공개 시뮬레이션 코드의 처리량이나 개선율은 합격 기준으로 사용하지 않는다. 외부 코드는 구조 참고 또는 독립 비교 도구로만 기록하고, 본 제품의 장비 물리 모델과 시험 결과는 자체 기준선으로 관리한다.

## 13. 개발 단계·일정·인력 계획

### 13.1 단계별 승인 기준

| Gate | 승인 내용 | 다음 단계 진입 조건 |
| --- | --- | --- |
| G0 | 기존 소스·버전·입력·규격·골든 사례 기준선, Rev.0 docx 프리셋 확정 | D01~D15 담당·처리 방향과 필수 지원 범위 승인 |
| G1 | 공통 커널·스키마·시험·플러그인 계약 | 단위·운동·직렬화·독립 검산 통과 |
| G2-i | 장비 i의 분석 계산 완료 | 근거식·입력·보고서·골든 검증 통과 |
| G3-i | 장비 i의 독립 프로그램 설정 완료 | 필수 DES·UI·저장·문서·배포·전용 시험 승인 |
| G3-All | 5종 모두 설정 완료 | G3-STC, G3-Linear, G3-Loop, G3-4Way, G3-Conveyor 모두 승인 |
| G4 | 단일 프로그램 통합 완료 | 단독판 결과 보존, 통합 프로젝트·보고서·오프라인 시험 |
| G5 | 연결 시스템 검증 완료 | 인계·공유 자원·질량수지·대표 라인 검증 |

장비별 “설정 완료”는 화면 입력이 가능하다는 뜻이 아니라, 지원하는 설정 범위에서 계산·시뮬레이션·저장·보고서·검증이 끝난 상태다. 미지원 기능은 버전·화면·보고서에 명시한다.

### 13.2 작업 분해 및 예상 공수

가정: 주개발자 1명 전담, 물류/기계 담당자의 주기적 산식·사양 검토, QA 지원, 기존 소스와 골든 데이터가 실제 확보되어 재사용 가능한 경우. 아래는 측정된 견적이 아닌 초기 계획 공수이며 1 MD는 개발자 1명의 1근무일이다.

| WBS | 작업 | 선행 조건 | 주개발자 공수 제안 |
| --- | --- | --- | --- |
| P0 | 소스·계보·골든 데이터 확보, 기준본·미해결 산식 정리, Rev.0 docx 본문 추출(D14) | 자료 전달 | 8~12 MD |
| P1 | 공통 커널(지연·보정계수·fleet 포함)·스키마·플러그인·SimulationCore·통계·ExperimentRunner·Optimizer·RenderModel·시험·빌드·기본 화면 | G0 | 18~26 MD |
| P2 | 직선 RGV 기준 플러그인 및 독립 프로그램 | G1 | 6~10 MD |
| P3 | 스태커크레인 자산 포팅·분석·장비 DES·3D 애니메이션·실험기·설계 탐색기·해석식 대조·검증(A5 0~11단계) | G1 | 24~34 MD |
| P4 | Loop RGV 계열 정리·결함 3건 수정·다중 차량 DES·검증 | G1, D04·D12 기준 정리 | 22~32 MD |
| P5 | 4-way Shuttle 산식 정리·교차로/리프트 DES·설계공간 정의 | G1, D01 기준 정리 | 26~38 MD |
| P6 | 컨베이어 코어 포팅·네트워크 DES·감도 분석·골든 회귀시험 | G1, D05·D13 기준 정리 | 18~26 MD |
| P7 | 5종 공통 회귀·시나리오 실험·문서·오프라인 배포·사용자 검수 | 각 G3 후보 | 12~18 MD |
| P8 | 통합 1차 AppShell·프로젝트·결과 동등성 | G3-All | 8~12 MD |
| P9 | 통합 2차 인계·연결 라인 DES·검증 | G4 및 대표 라인 정의 | 18~30 MD |
| 별도 P10 | 최적화·AI 파일럿(O2 이상) | 검증 엔진·실험 데이터·목표 확정 | 대상 선정 후 별도 산정 |

P0~P8 합계는 장비 단위 DES·실험기·STC 3D·설계 탐색기를 포함하여 **142~208 MD**다. v1.1 대비 P1에 통계·실험기·최적화기·렌더 모델 공통화 4~6 MD, P3에 3D 뷰·실험·최적화 탭 6~8 MD가 늘었다. v1.1 본문의 합계 129~190 MD는 당시 표의 실제 합(132~194 MD)과 달랐으므로 이번 판에서 표 기준으로 바로잡았다. 20% 기술·재시험 예비 공수를 포함하면 약 **170~250 MD**, 개발자 1명·주 5일 기준 **약 34~50주**다. 규격 원문·현장 데이터 대기, 구매·승인 지연, 담당자 휴무는 별도다.

A5는 STC 시뮬레이터 단독 규모를 JS 약 4.5k줄로 추정했다. 본 계획은 그중 rng·stats·runner·optimizer·scene·build를 P1 공통 모듈로 옮기므로 P3의 순수 STC 몫은 engine·kin·fem9851·validate·presets·view3d·app이다.

연결 시뮬레이션 P9는 기본 18~30 MD에 20% 예비를 더하면 약 22~36 MD, 약 4~8주를 추가하는 초기 가정이다. 복잡한 복선 루프, 동일 레일 다중 차량, tier-to-tier, 현장 WCS 연계가 필수로 추가되면 다시 산정한다.

목차와 제품 표시 순서는 사용자 요청을 유지한다. 구현은 기존 공통 커널의 출발점인 직선 RGV를 먼저 표준 플러그인으로 정리하면 다른 4종의 중복 작업을 줄일 수 있어 P2를 선행하는 순서를 제안한다. 여러 개발자가 참여하는 경우에도 G3-All 이전에 제품 통합을 시작하지 않는 원칙은 동일하다.

### 13.3 역할과 승인

| 역할 | 주요 책임 |
| --- | --- |
| 제품/프로젝트 책임자 | 우선순위, v1.0 지원 범위, 일정·변경 승인 |
| 물류·기계 검토자 | 사이클 정의, 장비 사양, 규격 해석, 실측 타당성 |
| 개발 책임자 | 공통 구조, 구현, 버전·빌드, 단위·회귀시험 |
| QA/검증 담당 | 독립 검산, 경계·현장 사례, 배포본 시험, 결함 추적 |
| 사용자 검수자 | 실제 설정·결과 해석·보고서·재현성 승인 |

소규모 팀에서 역할 겸임은 가능하지만 산식 작성자와 독립 검산자의 검토 이력은 구분한다. AI 보조로 작성한 결과도 동일한 승인 절차를 적용한다.

### 13.4 범위 변경 원칙

- 신규 설비 형식·동작 모드는 입력 필드만 추가하지 않고 계산·DES·시험·보고서 영향을 함께 평가한다.
- 변경마다 요청 내용, 근거, 예상 공수, 결과 호환성, 승인자를 기록한다.
- 공통 커널 변경은 5종 전체 회귀시험을 통과한 뒤 배포한다.
- 이미 승인된 계산 결과가 달라지면 changelog와 마이그레이션 안내에 이유·영향 사례를 남긴다.

## 14. 착수 시 필요한 자료와 위험 관리

### 14.1 요청 자료

| 우선순위 | 자료 | 용도 |
| --- | --- | --- |
| 필수 | 기존 5종 및 공통 커널의 소스·배포 파일·버전/커밋 | 실제 재사용 가능 범위 확인 |
| 필수 | 시험 코드·골든 JSON/CSV·Excel 원본·실행 로그 | 기재된 시험의 재현 |
| 필수 | 속도·가감속·정위치·이재·제어 지연 사양과 측정 근거 | 물리 입력 기준선 |
| 필수 | 레이아웃, 스테이션·리프트·버퍼 좌표와 용량 | 실제 이동·공유 자원 모델 |
| 필수 | 시간대별 입출고 또는 작업 단위 OD 이력 | 평균·피크·혼잡 검증 |
| 필수 | 초기 재고·SKU·위치·WIP, 입출고 정책 | 재고 부족·종료 상태 검증 |
| 규격 모드 필수 | 적용 FEM/VDI 등의 판본·조항·원문 접근 자료 | 표준 관련 표현과 골든 사례 확정 |
| 현장 검증 필수 | FAT/SAT 또는 운영 로그, 실제 처리량·대기·고장·정지 기록 | 모델 보정과 별도 검증 |
| STC 필수 | `STC 물동량 계산서(FEM 9.851 준용)_Rev0.docx` 원본, `stc_calculator.py` 실행 환경, MHKernel v0.1.0 원본 경로와 `kernel_sync_check.js` | 프리셋 확정(D14), 골든 JSON 생성, 커널 사본 동기 규칙 |
| 벤더 질의 | 표준 질의서 3항목: 가·감속도, 곡선 제한속도, 실측 이재시간(정지·위치결정·이재·출발 포함) | 비공개 사양의 입력 근거 확보. 견적·기술협의 시 첨부 [첨부 A4] |
| 선택 | 장비·버퍼·전력·유지비 자료 | 비용·에너지 목적함수 |
| 선택 | Calzavara et al. (2018) 본문, Bozer & Srinivasan (1991) 닫힌형 해석식, SEMI E84 | 혼잡 모델·Loop 해석식 근거 보강. 미확보 시 대체 근거를 표시 [첨부 A4] |

공유할 작업 로그의 최소 필드는 작업 ID, 도착시각, 입고/출고 구분, 출발·도착, 화물 단위·수량이다. 가능하면 작업 시작/완료, 차량·장비 ID, 대기 원인도 포함한다. 시간대·시계 오차·결측 처리 규칙을 함께 정하고 민감한 고객명·SKU는 가명화한다.

### 14.2 주요 위험과 대응

| 위험 | 영향 | 대응 / 승인 기준 |
| --- | --- | --- |
| 문서와 실제 코드 불일치 | 예상보다 큰 재개발 | P0에서 실행·버전·시험 재현 후 일정 재산정 |
| 산식·규격 해석 불확실 | 계산 근거에 대한 오해 | 원문 확인 전 “참고/검증 대기” 표시, 승인된 공학 모델과 구분 |
| 분석·DES 같은 오류 공유 | 잘못된 상호 일치 | 독립 검산·실측 대조 유지 |
| 공차·혼잡·가용도 중복 반영 | 설비 과대·과소 산정 | 시간·보정 항목별 적용 원장 |
| 재고·수요의 불균형 | 정상상태처럼 보이는 왜곡 | 초기/종료 재고·미완료·순유입 보고 |
| 4-way·Loop 경합 축소 | 다대수 처리량 과대 추정 | 필수 단독 DES와 점유·교착 시험 |
| 후속 통합 시 데이터 불일치 | 재작업·결과 변화 | 최초부터 공통 스키마·단위·포트 계약 |
| 지원하지 않는 옵션의 UI 노출 | 사용자의 잘못된 설계 판단 | 기능 목록·입력 차단·보고서 경고 |
| 외부 코드·데이터 조건 미확인 | 재사용·배포 제약 | 도입 전 버전·라이선스·데이터 권한 검토 |
| 시각화·AI 선행으로 범위 확대 | 검증·통합 지연 | 계산·필수 DES 우선. 3D는 STC 한정, 다른 장비 3D·학습은 별도 |
| three.js 확보 실패(회사망 CDN 차단) | STC 3D 빌드 불가 | npm pack 경로로 확보, SHA-256 기록. 3D 없이도 통계·실험 탭이 동작하도록 첫 마일스톤을 3D 이전에 둔다 [첨부 A5] |
| 커널 사본 불일치 | 장비별로 다른 t(s) 결과 | 사본마다 원본 경로·버전 기록, 동기 검사 스크립트 대상에 포함(9.1절) |
| 우선순위 규칙의 기아·재고 진동 | 결과 해석 오해 | 모델 거동으로 KPI·경고에 노출하고 숨기지 않음 [첨부 A5] |
| Drive 동기화 폴더 쓰기 충돌 | 빌드 실패 | 쓰기 재시도, 소형 산출물 |

이 프로그램은 설비 선정·물동량 검토를 지원한다. 물동량 계산 통과만으로 장비 안전성, 법규 준수, 계약 성능 보증을 판정하지 않는다. 안전·인수시험은 적용 규격·계약·현장 절차에 따라 별도 검토한다.

## 15. 최종 산출물과 “셋팅 완료” 체크리스트

### 15.1 장비별 공통 산출물

1. 단독 실행 HTML과 모듈형 원본 소스.
2. 입력 항목 정의서·단위·기본값 근거·지원/미지원 기능 목록.
3. 계산 근거서: 식, 변수, 동작 횟수, 규격/근사/실측의 구분.
4. 공통 JSON 스키마, 구버전 변환 규칙, 표준 시나리오 JSON.
5. 물동량 CSV 입력 예제와 필요 시 기존 Excel 호환 템플릿.
6. 단위·골든·시뮬레이션·배포본 시험과 검증 보고서.
7. 사용자 매뉴얼과 결과 보고서 템플릿.
8. 버전·커널 해시·변경 이력·알려진 제약.

기본 결과 보고서는 Markdown으로 제공하고 브라우저 인쇄/PDF 저장을 지원한다. 수식 포함 XLSX, 자동 서식 PDF는 기존 기능 재사용 가능성과 업무 필요를 확인하여 범위를 확정한다. 별도 보고서 계산식을 중복 유지하지 않도록 동일 결과 모델에서 생성한다. 벤더 협의용 Excel 양식(살아있는 수식) 내보내기/불러오기는 기존 컨베이어·STC 자산 확인 후 선택 범위로 둔다. [첨부 A4]

보고서 구조는 5종이 동일하게 다음 7개 절을 갖는다. [첨부 A4]

1. 입력 요약(제원·속도·지연·보정계수와 근거)
2. 사이클타임 분해(구간별·합계)
3. 달성 물동량과 요구 대비 여유율, 포화 여부 지표
4. 제약 검증(물리한계, 목표 부하율, 병목 위치)
5. 민감도 분석
6. 적용 규격과 준용 범위. RGV는 "FEM 9.860 준용, 규격 적용대상 아님"과 EN 619:2022를 기재(D11)
7. VDI 3561 Bl.4 경고문: "평균 사이클타임은 달성 가능 물동량에 대해 아무것도 말해주지 않는다." 사이클타임과 달성 물동량을 항상 함께 제시한다.

시뮬레이션 결과를 포함하는 보고서는 여기에 seed·반복 수·warm-up·실행가능 판정과 사유, 해석식 대조 Δ%를 추가한다.

### 15.2 장비별 승인 체크리스트

- [ ] 지원할 설비 구성·운영 모드·제약이 확정되었다.
- [ ] 모든 입력이 실제 계산/시뮬레이션에 반영되거나 미지원으로 명시된다.
- [ ] 단위·사이클·화물·입출고 집계가 공통 규격과 일치한다.
- [ ] 수식 기준본과 독립 검산·기존 골든 비교가 승인되었다.
- [ ] 필수 시뮬레이션에서 자원 점유·대기·차단·재고가 검증되었다.
- [ ] 동일 설정·수요·seed로 결과를 재현할 수 있다.
- [ ] 입력 저장/불러오기·오프라인 실행·취소·보고서가 동작한다.
- [ ] 실제 배포 HTML의 시험이 통과했다.
- [ ] 대표 사례의 결과와 미지원·미검증 항목을 사용자가 검토했다.
- [ ] 통합용 포트·자원 ID·공통 결과 계약이 통과했다.

**위 체크리스트를 5종 모두 통과한 시점을 통합 1차의 착수점으로 한다.**

### 15.3 통합 승인 체크리스트

- [ ] 단독판과 통합판의 승인 사례 결과가 동일하다.
- [ ] 5종의 설정·시나리오를 한 프로젝트로 저장하고 다시 읽을 수 있다.
- [ ] 동일 종류의 장비 여러 대를 고유 ID로 관리한다.
- [ ] 보고서에 전체 외부 처리량과 장비별 취급량을 구분한다.
- [ ] 연결 시뮬레이션은 G5 범위에서 인계·공유자원·버퍼 시험을 통과한다.
- [ ] 원시 입력·버전·seed·가정·제약으로 결과를 재현할 수 있다.

## 16. 출처 및 요구사항 추적

### 16.1 첨부 문서

| ID | 자료 | 주로 반영한 내용 |
| --- | --- | --- |
| A1 | STC_ASRS_통합_기술자료_v1.3_20260908(1).md | STC 분석·DES, 기존 자산, 물리·집계·재고·검증 쟁점, 후속 최적화 |
| A2 | PRD_물동량계산_통합플랫폼_v1.4_20260908.md | 5종 구분, 공통 커널, 오프라인 배포, 장비별 자산·수식·포팅·시험, MVP/후속 범위 |
| A3 | AI_ASRS_물류자동화_공개코드_자료조사_20260908.md | 공개 시뮬레이션·최적화 코드의 구조, 적용 한계, 라이선스·재현성 원칙 |
| A4 | PRD_물동량계산_통합플랫폼_v1.0_20260830.md | 규격 적용 원칙(9.860 준용·P1/P2·EN 619·VDI 경고), 지연 모델, 보정계수 A·F_t·E_w, 대수산정, Loop RGV 결함 3건과 혼잡·물리한계, 컨베이어 판정·감도, 골든 테스트 T1~T7, 금지사항, 벤더 질의서, 미해결 과제. v1.4보다 앞선 판이므로 v1.4·본 계획과 충돌하는 범위 정의(DES 비목표, 4종 대상)는 채택하지 않음 |
| A5 | STC_ASRS_FlexSim형_시뮬레이터_구현계획_v1.0_20260907.md | DES 엔진(힙·계획 실행기·연속 보간), 운동학 phases/posAt, 엔티티·정책·존 검증, 데이터 계약, 통계, Worker 풀, 최적화기, 3D 뷰, 탭 구성, 프리셋·검증 앵커, 구현 순서, 테스트 목록, 리스크. 독립 프로젝트로 계획된 것을 STC 플러그인 DES와 공통 시뮬레이션 모듈의 기준으로 편입 |

첨부 원본은 변경하지 않았다. A4는 DES를 비목표로 두었으나 v1.1 이후 본 계획은 장비 단위 DES를 필수로 하며, A5가 그 구현 설계를 제공한다. A5가 "독립 프로젝트"로 둔 위치(`01. Stacker Crane\stc-asrs-sim`)는 유지하되 공통 모듈은 통합 저장소 원본에서 복사·동기하는 규칙(9.1절)을 적용한다. 문서의 버전·시험 통과 기재와 실제 실행 재현을 구분했으며, 불일치 항목을 2.3절과 장비별 검증 과제로 전환했다. 장비 단위 DES를 필수 범위로 추가했지만, 실제 구현·실행 검증이 완료되었다는 의미는 아니다.

### 16.2 공개 자료와 확인 범위

공개 자료의 공식 링크 및 적용 판단은 11.4절 R1~R11에 수록했다. 확인일은 2026-09-08이며 저장소 설명·공식 문서에 대한 검토다. 실제 채택 시 대상 커밋·라이선스·의존성·실행 결과를 다시 고정한다.

FEM 9.851, FEM 9.860 및 첨부에서 언급한 다른 규격의 원문은 이번 첨부에 포함되지 않았다. 본 계획은 첨부의 규격 관련 기술을 요구사항으로 취급하되 세부 조항·좌표·허용오차의 규범적 확정은 G0의 확인 과제로 남긴다. 특히 FEM 9.860의 적용을 모든 4-way 경합·RGV 운행에 자동 확대하지 않는다.

### 16.3 요구사항-개발-검증 매핑

| 요구사항 | 구현 계획 | 검증·승인 |
| --- | --- | --- |
| 5개 프로그램을 나누어 개발 | 4~8절, 장비별 플러그인·독립 배포 | G3-i |
| 5개 모두 설정 후 하나로 통합 | 10.1~10.2절 | G3-All 이후 G4 |
| AS/RS 물동량 계산 검증 | 3~4절, 7절, 12절 | K01~K06, 장비별 골든·현장 대조 |
| 장비별 물류 이동 시뮬레이션 | 4~9절 S0~S2, 장비별 DES·반복 실험 | S01~S06, V1~V3, 12.6 |
| 통합 라인 동적 시뮬레이션 | 10절 S3, 포트·공유자원·차단·교착 모델 | I01~I02, G5, 12.6 |
| 통합 전체 물류 흐름 검증 | 10.3~10.4절 | I01~I02, G5 |
| 기존 개발 자산 활용 | 2.2~2.3절, 장비별 포팅 | G0, P02 |
| 공개 시뮬레이션·AI 코드 참고 | 11절 R1~R11 | 구조 참고·라이선스·재현성·적용 한계 검토 |
| 계산 결과의 추적·보고 | 3.5절, 9.4절, 15절 | P01~P03, 사용자 검수 |
| FlexSim형 4요소(애니메이션·통계·실험기·최적화기) | 1.1절, 4.2절, 9.4~9.6절, 11.1절 O1 | S05·S07~S12, E01~E02, O02, P04 |
| 규격 준용 표기·안전규격 정정 | D11, 5.3절, 15.1절 보고서 6절 | 보고서 문구 검수 |
| 보정계수·대수산정 공통화 | 3.4절, D15 | K08, 민감도 표 |
| 문헌 근거 금지사항 | 3.6절 | 코드 리뷰 체크리스트, K07·K09 |

## 17. 착수 권고안

첫 개발 작업은 신규 화면 제작이 아니라 **기존 소스·골든 데이터의 실제 확보와 기준선 확정**으로 한다. 이어 공통 커널·스키마를 고정하고, 직선 RGV를 기준 플러그인으로 정리한 뒤 5종의 독립 완성을 진행한다.

착수 회의에서는 다음 네 가지를 우선 결정하면 된다.

1. 5종 v1.0의 실제 설비 형식: 공유레일 다중 차량, 복잡 루프, tier-to-tier 등의 포함 여부.
2. 장비별 기준 소스와 시험 데이터, 규격 모드에 필요한 원문.
3. 목표 물동량·피크 조건·대기시간·버퍼·현장 검수 기준.
4. 통합 1차와 연결 시뮬레이션 2차의 납품 구분, 담당자와 투입 가능 인력.

이 기준이 정해지면 본 계획의 공수·검증선을 확정 일정으로 전환하고, 5종 각각의 승인 이력을 유지한 채 단일 프로그램으로 통합한다.

시뮬레이션 구현은 STC를 첫 사례로 한다. P1에서 공통 엔진·통계·실험기·최적화기·렌더 모델을 만들고, P3에서 A5의 0~11단계 순서대로 STC DES와 3D 뷰를 완성하면서 공통 모듈을 실전 검증한다. 다른 4종은 검증된 공통 모듈 위에 장비 물리 모델과 2D 뷰만 추가한다.

## 부록 B. v1.2 반영 대조표

첨부 A4·A5의 항목이 본 계획 어디에 반영되었는지, 채택하지 않은 항목은 왜인지 정리한다.

| 출처 | 항목 | 반영 위치 | 처리 |
| --- | --- | --- | --- |
| A4 §3.2 ① | FEM 9.860 준용 표기, transfer carriage·AGV 제외 | D11, 5.3절 8, 15.1절 | 채택 |
| A4 §3.2 ② | P1/P2 좌표 혼용 금지(9.851 vs VDI 3561) | 3.6절 #9, 4.3절 3 | 채택. 원문 대조는 D02 유지 |
| A4 §3.2 ③ | ISO 3691-4 적용 불가, EN 619:2022 | D11 | 채택 |
| A4 §3.2 ④ | VDI 3561 Bl.4 경고문 | 3.6절 #10, 15.1절 7 | 채택 |
| A4 K1 | jerk 두 방식, creep 경계 연속, a = d 식(3) 일치 | 3.2절, K07 | 채택 |
| A4 K1 | 곡선 구간 분할 후 각각 t(s) 합산 | 3.2절 | 미채택. 본 계획은 경계속도 연결 방식이 더 정확하므로 유지. 분할 합산은 보수적 근사 모드로만 허용 |
| A4 K2 | 지연 항목 8종, 간편/상세 동일 기본값 | 3.2절 지연 모델, K10 | 채택 |
| A4 K3 | Chebyshev 동시동작 | 3.2절 makeMove | 이미 반영, 빠른 축 정지 유지 추가 |
| A4 K4 | A·F_t·E_w, 기본값 1.0 금지, 민감도 기본 | 3.4절, D15 | 채택. 가용도 1 "이상 조건" 규칙과 통일 |
| A4 K5 | w·WL·AT·AN·θ_N·ρ, FEM 9.860 §5.6 | 3.4절 대수산정 커널, K08 | 채택 |
| A4 K6 | 보고서 7개 절 | 15.1절 | 채택 |
| A4 K7 | major.minor 버전 경고, Excel 양식 | 3.5절, 15.1절 | 채택(Excel은 선택) |
| A4 §6.1 | STC 14항목, Double Deep tREL, Twin Fork, 9.860 옵션 병기 | 4.2절 | 채택. Double Deep DES 재배치는 후속 |
| A4 §6.2 | 거리행렬, Tandem, T_L/T_U 실측 | 5.3절 6~7 | 채택 |
| A4 §6.3 | Loop 결함 3건, 1랩 법칙, η(N), N_max·0.3 경고, 토폴로지, ρ ≤ 0.7~0.8 | D12, 6.2~6.4절, 3.4절 | 채택. ρ 기준은 설정값+근거 표시로 완화 |
| A4 §6.4 | 존 수·버퍼·적체·starvation·공용 장치·4단계 판정·감도 분석 | 8.4절 7~10, D13 | 채택. 이름 기반 공유는 호환 모드로 한정 |
| A4 §7 | 5%/10% 기준, 실측 5회 평균, T1~T7, DAMBACH 교차검증 | 12.3절, K07~K09, 6.3절 11, V01 | 채택 |
| A4 §8 | 금지사항 9건 | 3.6절 | 채택, VDI 경고 추가로 10건 |
| A4 §10~11 | 벤더 질의서, 미해결 문헌 | 14.1절 | 채택 |
| A4 §2.2 | DES·4-way·라인 연계 비목표 | — | 미채택. v1.1 이후 범위와 충돌 |
| A5 §1 | FlexSim 4요소 | 1.1절, 9.6절 | 채택 |
| A5 §2 | 재사용 자산 경로 | 2.2절, 14.1절 | 채택 |
| A5 §3 | 모듈 구성(IIFE, 엔진/앱 블록, vendor) | 1.3절, 9.3절 | 채택 |
| A5 §4.1 | 힙 (t, seq), 계획 실행기, advanceTo/posAt 재생, CRN, Math.random 차단 | 9.5절 7~12, 4.3절 9 | 채택 |
| A5 §4.2 | axisTime phases, makeMove, forkCycle, 해석식 동일 커널 | 3.2절, 4.3절 8 | 채택 |
| A5 §4.3 | 랙·스테이션·존·정책·상태·설계 변수 | 4.2절, 4.3절 9~12 | 채택 |
| A5 §4.4 | scenario/rep/ScenarioResult/DesignSpace 계약, 실행가능 판정, 목적 | 3.5절, 9.5절 10 | 채택, 장비별 스키마 이름으로 일반화 |
| A5 §4.5 | 3D 뷰 | 9.3절, P04 | STC 한정 채택 |
| A5 §4.6 | 탭 7개 | 9.4절 | 채택 |
| A5 §5 | 프리셋·검증 앵커 | 4.3절 프리셋 표 | 채택, 범용 기본값 금지 유지 |
| A5 §6 | 구현 순서 0~11 | 4.3절 구현 순서 | 채택 |
| A5 §7~8 | 검증·테스트 목록 | 4.4절, 12.2절 S07~S12·E01~E02·O02·P04 | 채택 |
| A5 §9 | 확인 사항 4건 | D14 | 채택 |
| A5 §10 | 리스크 | 14.2절 | 채택 |
