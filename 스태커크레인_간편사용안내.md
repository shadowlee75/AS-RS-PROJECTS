# 스태커크레인 간편 계산 사용 안내

실행 파일: **스태커크레인_간편실행.html** · 간편판 v1.0.0

파일을 더블클릭하면 브라우저에서 실행됩니다. 인터넷 연결이나 설치가 필요하지 않습니다. 기존 **스태커크레인_실행.html**은 그대로 사용할 수 있습니다.

## 처음 계산하는 순서

1. **기본 입력**에서 랙 길이·높이, 가로 칸 수·단 수, 보관면·깊이를 입력합니다.
2. 주행·승강·포크 속도를 **m/min**으로 입력합니다. 1 m/s는 60 m/min입니다. 앞열 포크 편도 거리는 통로 중심부터 화물 중심까지의 총 거리입니다.
3. 크레인 대수와 **전체 크레인을 합친** 입고·출고 요구량을 입력하고 운전 방식을 선택합니다.
4. **물동량 계산하기**를 누릅니다. 상단의 **효율을 반영한 처리량**을 먼저 확인합니다.
5. 상세 사양을 알고 있다면 **파라미터 설정**에서 조정한 뒤 다시 계산합니다. 변경한 입력과 이전 계산 대비 처리량 차이가 표시됩니다.

모든 초기값은 조작과 설계 검토를 위한 **예시값**입니다. 실제 장비 사양이나 현장 조건에 맞춰 입력하세요.

## 혼동하기 쉬운 세 가지

| 항목 | 초기값 | 의미 |
| --- | ---: | --- |
| 운전효율 | 85% | 시간당 이론 처리량에 0.85를 곱합니다. 85%는 `85`로 입력합니다. |
| 적재 시 속도·가감속 적용률 | 100% | 입력 사양 그대로 움직입니다. 50%이면 적재 시 주행·승강 속도와 가감속에 0.5를 곱해 이동이 느려집니다. 캐리지 이재 승하강에도 적용합니다. |
| 제어 지연 | 0.5초/사이클 | 사이클마다 한 번 추가하는 시간입니다. 다른 조건이 같으면 줄일수록 처리량이 증가합니다. |

속도 입력값이 이미 적재 상태의 사양이면 **적재 사양 적용률을 100%**로 두세요. 이 값을 낮추는 것은 지연시간을 줄이는 것과 다릅니다.

**목표 설계 부하율**의 초기값은 85%입니다. 필요 크레인 대수와 여유를 판단하는 기준이며, 표시 처리량에 추가로 곱하지 않습니다. **피크 계수** 초기값은 1배로, 요구량을 추가 증가시키지 않습니다.

## 파라미터를 별도로 저장하기

- **프로젝트 저장**: 기본 입력과 파라미터를 모두 저장합니다.
- **파라미터 설정 → 설정 저장**: 효율·가감속·이재·지연·상세 랙 치수·포트 위치·계산 방법만 저장합니다.
- **설정 불러오기**: 다른 프로젝트에도 같은 파라미터를 적용합니다. 프로젝트명, 랙 길이·높이·칸 수, 장비 속도, 앞열 거리, 요구량, 크레인 수, 운전 방식은 유지됩니다.
- **파라미터만 초기화**: 상세 조건만 간편판 예시값으로 되돌립니다.

저장 형식은 JSON입니다. 설정을 불러온 후 **물동량 계산하기**를 눌러 새 결과를 확인하세요. 입력을 바꾸기만 하면 이전 결과에 변경 안내가 표시됩니다.

## 결과 읽기

- **1대 처리량**: 운전효율을 반영한 1대의 총 취급량입니다. 입고와 출고를 합산합니다.
- **전체 처리능력**: 모든 독립 통로 크레인의 처리량 합계입니다.
- **필요 대수**: 피크 요구량과 목표 설계 부하율을 기준으로 산정합니다.
- **단독/복합 비교**: SC 1회는 화물 1개, DC 1회는 입고 1개와 출고 1개입니다. 비교표의 처리량은 각 사이클을 연속 운전한 환산값입니다. 상단 결과는 선택한 운전 방식과 입출고 수요 비중을 반영하므로 다를 수 있습니다.

운전효율은 시간당 보정 처리량에 적용하며 물리적 사이클 시간은 바꾸지 않습니다. 같은 조건에서 결과가 달라지면 변경 내역과 적용 파라미터를 함께 확인하세요.

## 상세 치수와 계산 방법

- 1단 높이와 단간격은 빈칸이면 자동 산정합니다. 실제 치수가 있으면 입력하세요.
- 더블딥 뒷열 거리는 통로 중심부터 뒷열 화물 중심까지의 총 거리입니다. 앞열보다 커야 하며 빈칸은 앞열의 2배입니다.
- 기본은 전체 보관 셀 평균입니다. FEM 비교가 필요하면 파라미터의 계산 방법을 FEM으로 바꾸고 CASE를 선택한 뒤 **선택한 배치 좌표 적용**을 누르세요. 실제 도면과 다른 좌표는 직접 수정할 수 있습니다.
- 기본 포트는 랙 하단의 공용 입출고 위치이며, 모든 STC에 같은 배치를 사용합니다.

## 기존 프로그램과 함께 사용하기

간편판의 계산 코어는 기존 v0.3.4, 운동 커널 v0.2.4와 동일합니다. **모든 계산 조건이 같을 때 결과도 같습니다.** 간편판은 초기 효율 85%, 피크 1배 등 예시 설정이 기존 초기값과 일부 다릅니다.

간편판 프로젝트 파일을 기존 프로그램에서 열 수 있습니다. 기존 프로젝트도 동일 포트 배치이면 읽을 수 있습니다. 기존 파일의 직접 운전효율 또는 A·Ft·Ew 적용값은 유지됩니다. 직접 효율을 입력하면 세부 계수를 대신합니다. STC별 개별 포트·복합 배치가 있는 파일은 기존 상세 프로그램에서 사용하세요.

간편판은 평균 사이클과 물동량 검토용입니다. 재고·대기·3D 시뮬레이션, STC별 복합 배치는 기존 상세 프로그램에서 확인하세요. 더블딥은 앞열·뒷열 균등 평균이며 화물 재배치·SKU 지정 출고는 계산에 포함하지 않습니다.

**보고서 / PDF**로 인쇄용 보고서를 열 수 있고, **상세 계산서 저장**으로 Markdown 계산서를 저장할 수 있습니다.
