# 컨베이어 물동량 계산 · 3D 시뮬레이션 사용 설명서

버전 **1.0.0** · 2026-09-15 · Flow Studio / Conveyor

## 1. 실행

같은 폴더의 **[컨베이어_실행.html](컨베이어_실행.html)**을 Edge 또는 Chrome에서 엽니다. 파일 하나에 계산 엔진, 3D 라이브러리와 화면이 포함되어 있으므로 서버·설치·인터넷 연결이 필요하지 않습니다. 입력 데이터는 외부로 전송하지 않습니다.

`06_conveyor`는 개발 원본과 검증 자료입니다. 실행 HTML만 다른 PC로 복사할 수 있습니다. 브라우저가 WebGL을 제공하지 않으면 2D 재생으로 전환하며 안내를 표시합니다.

마지막으로 계산한 조건은 같은 브라우저에서 자동 복구를 시도합니다. 확실한 보관·PC 간 이동에는 **프로젝트 저장**으로 JSON 파일을 저장하세요. 자동 복구 저장은 브라우저의 로컬 파일 정책에 따라 불가능할 수 있습니다.

## 2. 빠른 사용 순서

1. 왼쪽에서 **직선 / T 합류 / T 분기 / + 교차 / H 연결 / U 회송 / 우회 루프**를 선택합니다.
2. **레이아웃 편집**에서 노드 좌표와 연결 방향, 구간 속도·피치·폭·허용하중을 수정합니다.
3. **운반물 · 수요**에서 팔레트·박스·토트의 크기·중량 및 OD별 시간당 수요를 입력합니다.
4. **운영 조건**에서 연속/ZPA, 초기 점유, 계획 정지시간을 설정합니다.
5. 운전시간·준비운전·도착 방식·난수 시드를 확인하고 **설정 적용 · 계산**을 누릅니다.
6. **물동량 · 3D**에서 결과를 확인하고 ▶ 버튼으로 기록을 재생합니다.
7. **결과 · 비교**에서 병목·차단·가동률과 대안을 비교합니다.
8. 프로젝트 JSON, 결과 CSV, 인쇄용 HTML 보고서를 저장합니다.

템플릿은 현재 입력을 새 예제로 교체합니다. 변경한 설계를 유지하려면 먼저 저장하세요. 기존 스태커 크레인·직선/LOOP RGV·셔틀·AGV/AMR 실행 파일은 변경하지 않습니다.

## 3. 단위

| 입력/출력 | 화면 단위 | 내부·프로젝트 단위 |
|---|---|---|
| 노드 X, Z, 컨베이어 상면 높이 | m | m |
| 화물 길이·폭·높이, 구간 폭·피치·최소 간격 | mm | m |
| 컨베이어 속도 | m/min | m/s |
| 가속·감속 | m/s² | m/s² |
| 이재·후단 인계·존 정지시간 | s | s |
| 운전·준비운전·계획 정지 | min | s |
| 화물 중량·허용하중 | kg | kg |
| 수요 및 합계 처리량 | 개/h, HU/h | handling-unit/h |

**HU는 운반물 1개**입니다. 혼합 운전의 합계는 팔레트·박스·토트 개수를 더한 값입니다. PLT/h, box/h, tote/h를 별도로 제공합니다. 자동 팔레타이징·박스 환산·적재분할은 수행하지 않습니다.

예제 수치와 부하 판정 기준은 모델 검토용 가정값이며 현장 설비의 보증값이 아닙니다.

## 4. 배치와 구간

### 노드

- **입구:** 외부 요청을 받아 컨베이어로 투입합니다. 진입 구간은 연결하지 않습니다.
- **이재/합류:** 합류, 분기, 방향 전환에 사용합니다. 한 번에 화물 1개를 처리합니다.
- **출구:** 입력한 인계주기만큼 점유한 뒤 화물을 완료 처리합니다. 진출 구간은 연결하지 않습니다.
- **이재/인계 주기:** 정위치·방향 전환·인계 등을 합친 화물 1개당 총 시간입니다. 별도 추가시간과 중복 합산하지 마세요.
- **공유 자원 ID:** 여러 노드가 같은 ID를 참조하면 장치 1대를 공유합니다. 표시 이름은 공유 기준이 아닙니다.

좌표는 표에서 직접 수정하거나 2D 편집 화면에서 노드를 드래그할 수 있습니다. 드래그는 0.5 m 단위입니다. 구간 길이는 두 노드의 3차원 거리로 계산합니다.

### 구간

- 각 구간은 **시작 → 끝**의 단방향입니다.
- 양방향 설비는 실제 분리된 레인·좌표와 방향으로 모델링합니다. 같은 위치의 중복/역방향 링크는 허용하지 않습니다.
- `존 수 = floor(구간 길이 / 존 피치)`입니다. 존당 화물 1개이며 최소 1개가 필요합니다.
- 구간을 사용하는 모든 화물에 대해 `화물 길이 + 최소 간격 ≤ 존 피치`, `화물 폭 + 100 mm ≤ 구간 폭`, `화물 중량 ≤ 허용하중`을 검사합니다. 이 여유는 본 모델의 입력 적합성 조건입니다.
- 롤러·벨트·체인은 표시 형식입니다. 계산은 속도·운전 방식·존·주기가 결정합니다. 체인 동력이나 벨트 장력은 계산하지 않습니다.
- 서로 교차하는 평면 중심선에는 **공통 이재 노드**가 있어야 합니다. 노드를 공유하지 않는 교차를 자동으로 합류로 해석하지 않습니다.
- 높이가 다른 선은 경사 구간으로 표시할 수 있습니다. 경사 동력·미끄럼·화물 자세는 계산하지 않습니다. 승강 리프트의 왕복 사이클 모델은 포함하지 않습니다.

**H 템플릿:** 두 개의 평행 이송 라인과 단방향 연결 구간을 제공합니다. 연결 방향은 구간의 시작/끝을 바꾸고 해당 방향에 맞춰 OD를 수정합니다.

**우회 루프 템플릿:** 느린 직통 구간과 우회 경로를 비교하는 연결망입니다. 경로는 무부하 소요시간 기준으로 한 번 선택하며, 운전 중 혼잡·정지에 따른 동적 우회나 의도적 반복 순환은 하지 않습니다.

## 5. 운반물과 OD 수요

OD는 입구에서 출구까지의 작업 종류입니다. OD마다 화물 유형·시간당 수요·우선순위를 지정합니다. 동일 입구에서 여러 출구로 나가는 수요를 입력하면 각 수요의 비율이 분기 비율이 됩니다.

예: IN1 → OUT1 300개/h, IN1 → OUT2 200개/h이면 60:40 분기입니다. 서로 다른 화물 유형을 각 OD에 지정할 수 있습니다.

고정 경로는 **구간 무대기 통과시간 + 도착 노드 주기**의 합이 가장 작은 경로입니다. 라우팅은 실제 혼잡을 보고 화물을 다른 출구로 임의 전환하지 않습니다. 같은 구간 안에서 추월하지 않으므로 출구가 막힌 선두 화물 때문에 후속 화물도 기다릴 수 있습니다.

### 도착 방식

- 일정 간격: 첫 요청은 0초, 이후 `3600 / 시간당 수요` 간격입니다.
- Poisson: OD마다 독립적인 시드 기반 난수 흐름으로 간격을 생성합니다. 같은 입력·시드·프로그램 버전이면 결과가 같습니다.
- 확률 도착의 실제 요청 개수는 평균 수요와 다를 수 있습니다. 분기 비율은 평균 기준입니다.
- 요청이 처리되지 못하면 외부 미투입 대기열에 남습니다. 미투입 대기열은 무제한 요청 목록이며 물리 컨베이어 버퍼가 아닙니다.

### CSV 작업 이력

화면의 **CSV 양식**을 내려받아 아래 열을 유지하세요.

```csv
id,release_s,flowId
TASK-001,0,OD1
TASK-002,12,OD1
TASK-003,18,OD2
```

작업 1행은 화물 1개이며 발생 시각은 전체 운전 시작 이후 초입니다. `flowId`의 화물·출발·도착 정보를 사용합니다. 작업 ID는 중복될 수 없고 발생시각은 `[0, 운전 종료)`여야 합니다. 가져오면 수요 발생 기준이 **작업 이력**으로 바뀝니다. OD 시간당 수요를 이중으로 생성하지 않습니다.

## 6. 운영과 계산 방법

### 연속 이송

속도 `v` m/s, 피치 `p` m일 때:

```text
방출 최소 간격 headway = p / v        [s/개]
이론 구간 능력         = 3600 v / p   [개/h]
무대기 통과시간        = 구간 길이 / v [s]
```

여러 화물이 동시에 같은 구간을 이동합니다. 전체 길이를 이동해야 다음 화물이 투입되는 단일 장치로 계산하지 않습니다. 연속 구간의 가감속은 생략합니다.

### ZPA 정지 존

`존 길이 = 구간 길이 / 존 수`로 균등 분할합니다. 각 존마다 정지 상태에서 가속, 필요 시 정속, 감속, 정지 지연을 적용합니다.

```text
존 headway = 존 길이의 가감속 이송시간 + 존 정지시간
구간 무대기 통과시간 = 존 수 × 존 headway
```

가속·감속·정지시간은 ZPA에서 계산에 영향을 줍니다. 연속 이송 구간에서는 사용하지 않습니다. 이 모델은 유한 버퍼와 방출 간격으로 계산하는 구간 수준 모델이며 존 센서·모터별 PLC 스캔을 모사하지 않습니다.

### 합류 정책

- FIFO: 들어온 요청 시각이 빠른 화물이 먼저입니다. 동률은 생성 순번입니다.
- 교대 합류: 현재 이재 가능한 입력 구간을 번갈아 처리합니다.
- 고정 우선순위: 숫자가 큰 OD를 먼저 처리합니다. 동률이면 FIFO입니다.

정책은 각 입력 구간의 **선두 화물** 사이에서 적용됩니다. 선두를 추월하지 않습니다. 다른 노드가 같은 자원을 공유하면 노드 간 경합은 요청 시각·생성 순번 기준입니다.

### 버퍼와 예약

이재 시작 전에 후단 구간의 용량과 진입 간격을 확인하고 자리를 예약합니다. 이재 중에는 이재 자원과 후단 예약이 유지됩니다. 완료 후 후단 구간의 실제 점유로 전환합니다. 후단이 가득 차면 상류 화물을 이동시키지 않습니다.

초기 점유는 구간 하류부터 피치 간격으로 배치합니다. 각 구간에 초기 화물 유형과 최종 출구를 지정합니다. 초기 점유가 구간 용량보다 크거나 출구까지 화물을 통과시킬 수 없으면 실행을 막습니다.

### 계획 정지

선택한 구간의 이동과 투입·배출을 정지시키며 진행 중 이송시간도 그만큼 연장됩니다. 겹치는 정지 구간은 합집합으로 처리하여 중복 차감하지 않습니다. 정지 종료 후 대기 조건에 따라 운전을 재개합니다.

## 7. 결과 해석

전체 운전시간에는 준비운전이 포함됩니다. `측정시간 = 운전시간 − 준비운전`입니다. 준비운전이 끝나도 화물·대기열을 초기화하지 않습니다.

| 지표 | 의미 |
|---|---|
| 측정 처리량 | `[준비운전 종료, 전체 운전 종료)`에 완료한 개수 ÷ 측정시간 |
| 해석 처리능력 상한 | 현재 OD 구성·자원 방문 비율을 유지할 때 첫 자원이 포화되는 정상상태 처리량 |
| 요구 부하율 | 자원별 입력 수요 ÷ 해석 능력 |
| 미투입 | 발생했으나 입구 이재를 시작하지 못한 요청 |
| 재공 WIP | 입구 이재를 시작했거나 초기 배치되어 아직 완료하지 못한 화물 |
| 평균 점유·예약 | 실제 구간 점유 + 이재 중 후단 예약의 시간가중 평균 |
| 출구 대기비율 | 구간 선두가 이송·방출간격을 충족했으나 다음 이재를 시작하지 못한 시간 비율. 구간 정지시간은 제외 |
| 가동률 | 이재 자원을 점유한 시간 ÷ 측정시간 |
| 리드타임 | 요청 발생부터 출구 인계 완료까지. 측정 구간에 완료한 신규 화물만 대상이며 초기 화물 제외 |
| P95 리드타임 | 해당 완료 작업 리드타임의 최근접 순위 방식 95백분위수 |

완료 작업이 없으면 리드타임은 `—`입니다. 낮은 입력 수요에서 달성한 처리량은 최대 설비 능력이 아닙니다. 계획 정지와 버퍼 차단은 DES에 적용되며 해석 상한에는 포함하지 않습니다.

운전 종료 순간의 누적 수량은 종료 시각까지 포함합니다. 그 시각에 정확히 완료하는 작업은 누적 완료·화물 보존에는 포함되지만 종료 시각을 제외하는 처리량 측정 창에는 포함되지 않습니다.

항상 다음 수지를 확인합니다.

```text
총 요청 + 초기 재고 = 총 완료 + 재공 + 미투입
```

목표 부하율(기본 85%) 이하 적정, 95% 이하 주의, 100% 이하 위험, 100% 초과 처리불가로 표시합니다. 목표는 10~95% 사이에서 바꿀 수 있습니다. 시간 내 남은 미투입량과 대기시간도 함께 검토하세요.

## 8. 3D 재생

- ▶ / Ⅱ: 재생·일시정지
- ↺: 0초로 이동
- 시간 슬라이더: 원하는 시각으로 앞뒤 탐색
- 1×~120×: 표시 재생 배속
- 3D / 평면 / 시점 초기화: 카메라 조작
- 마우스 드래그 회전, 휠 확대, 우클릭 이동
- 구간 클릭: 해당 구간 편집 표로 이동
- 화면 저장: 재생 시각의 PNG 다운로드

**계산 결과와 운전 기록을 먼저 만든 뒤 재생**합니다. 배속이나 카메라가 처리량에 영향을 주지 않습니다. 상단 KPI는 전체 계산 결과이고, 화면 왼쪽 위의 완료·재공은 현재 재생 시각의 값입니다.

3D는 구간의 평균 이송 속도와 대기 위치를 표현합니다. ZPA 존마다의 정확한 가감속 궤적이나 실제 센서 신호는 표시하지 않습니다. 물리 형상 간섭을 판정하는 CAD 도구가 아닙니다. 한 번에 최대 600개를 표시하며 초과 시 화면에 알립니다. 계산 집계는 전체 화물을 포함합니다.

## 9. 비교와 저장

- **현재 결과 추가:** 비교 목록에 최대 8개 저장합니다. 측정시간·시드·입력 해시를 함께 표시합니다.
- **5가지 조건 비교:** 기준안, 구간 속도 +20%, 이재부 주기 −20%, 출구 주기 −20%, 계획 정지 제거를 같은 수요·시드로 각각 계산합니다.
- 입력을 바꾸면 이전 결과에 경고를 표시하고 결과 내보내기·비교를 막습니다. 다시 계산해야 현재 조건의 결과로 저장됩니다.
- **보고서·인쇄:** 내려받은 HTML 보고서를 열고 인쇄 / PDF 저장을 선택합니다.
- **프로젝트 JSON:** 재현 가능한 모든 입력을 저장합니다.
- **결과 CSV:** Excel에서 열 수 있는 UTF-8 BOM 파일입니다. 프로젝트 입력을 복원하는 파일은 아닙니다.

## 10. 다른 설비 연동

설비 연동 화면에서 모듈 ID와 입출구의 연결 대상 유형·모듈 ID를 입력합니다.

지원 식별자:

| 설비 | deviceType |
|---|---|
| 컨베이어 | conveyor |
| 스태커 크레인 | stacker-crane |
| 직선 RGV | rgv_linear |
| LOOP RGV | rgv_loop |
| 셔틀 | shuttle_4way |
| AGV & AMR | agv_amr |

**현재 연결 대상 입력은 메타데이터**입니다. 처리 능력은 출구 인계주기 입력으로 모사하며 연결 대상 프로그램을 직접 실행하거나 속도·대수를 읽지 않습니다. **연동 데이터 저장**은 포트, SI 단위, 입력 설정, 완료 이력, 화물 ID, 지표를 JSON으로 제공합니다.

후속 통합에서는 공통 시계, 후단 공간 예약, 화물 소유권 인계, 공유 자원, 이재시간 소유권을 연결해야 합니다. 개별 설비 처리량을 단순 합산하여 전체 시스템 처리량으로 사용하지 마세요.

개발 계약은 [06_conveyor/연동규격.md](06_conveyor/연동규격.md)에 정리했습니다.

## 11. 범위와 한계

- 이번 구현은 신규 컨베이어 모델입니다. 기존 계획서가 언급하는 cvcore/Excel 호환 포팅이나 2,617개 과거 골든 결과 재현은 수행하지 않았습니다. 해당 기준 원본은 이번 작업 폴더에서 확인되지 않았습니다.
- 고정 방향·고정 OD 경로, 유한 구간 버퍼, 화물당 존 1개를 지원합니다. 구간에서 혼합 화물도 같은 피치를 사용합니다.
- 연속 구간의 정밀 추종 제어, 충돌 외곽 검사, 벨트 장력·모터 용량·하중별 속도 저감, 동적 재순환, 리프트 기구학, PLC 통신은 포함하지 않습니다.
- 무작위 고장 대신 계획 정지시간을 입력할 수 있습니다. 확률 결과의 다회 반복 신뢰구간은 제공하지 않습니다.
- 화면 입력 상한은 노드 100, 구간 180, OD 150, 화물 종류 12, 작업 이력 30,000건, 초기 화물 3,000개입니다. 구간 존 수는 최대 600입니다. 큰 조건에서는 계산이 길어질 수 있고 이벤트 1,500,000건을 초과하면 명시적 오류로 중단합니다.

기본 검증 내역: [06_conveyor/검증결과.md](06_conveyor/검증결과.md).
